<UserControl
    x:Class="InfoPanel.Views.Components.DisplayItems"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:app="clr-namespace:InfoPanel"
    xmlns:components="clr-namespace:InfoPanel.Views.Components"
    xmlns:converters="clr-namespace:InfoPanel.Views.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dd="urn:gong-wpf-dragdrop"
    xmlns:local="clr-namespace:InfoPanel.Views.Components"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:InfoPanel.Models"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <UserControl.Resources>
        <!--  CollectionViewSource for filtering display items  -->
        <CollectionViewSource x:Key="DisplayItemsViewSource" Source="{Binding Source={x:Static app:SharedModel.Instance}, Path=DisplayItems}" />

        <DataTemplate x:Key="SimpleItemTemplate_MarginA">
            <local:DisplayItemView Width="375" Margin="10,0,30,0" />
        </DataTemplate>

        <DataTemplate x:Key="SimpleItemTemplate_MarginB">
            <local:DisplayItemView Width="365" Margin="0,0,10,0" />
        </DataTemplate>

        <DataTemplate x:Key="GroupItemTemplate">
            <Expander
                Margin="0,0,0,0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                IsExpanded="{Binding IsExpanded}">

                <Expander.Header>
                    <Grid Margin="0,-8,0,-10" HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="auto" />
                            <ColumnDefinition Width="auto" />
                        </Grid.ColumnDefinitions>
                        <!--  Group Header  -->
                        <Border
                            Grid.Column="0"
                            Background="#00FFFFFF"
                            PreviewMouseLeftButtonDown="Border_MouseDown">
                            <Grid HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="auto" />
                                </Grid.ColumnDefinitions>
                                <ui:SymbolIcon
                                    Width="20"
                                    Height="20"
                                    Margin="0,0,0,0"
                                    VerticalAlignment="Center"
                                    Symbol="Group24" />
                                <TextBlock
                                    Grid.Column="1"
                                    MaxWidth="180"
                                    Margin="5,2,0,5"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    FontSize="12"
                                    Text="{Binding Name}" />
                                <TextBlock
                                    Grid.Column="2"
                                    Margin="5,2,0,5"
                                    VerticalAlignment="Center"
                                    FontSize="12"
                                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                    Text="{Binding DisplayItemsCount, StringFormat={} {0} Items}" />
                            </Grid>
                        </Border>

                        <Button
                            Grid.Column="1"
                            Width="20"
                            Height="20"
                            Margin="40,0,-5,0"
                            Padding="0"
                            VerticalAlignment="Center"
                            Background="Transparent"
                            BorderThickness="0"
                            Command="{Binding ToggleLockCommand}"
                            Cursor="Hand"
                            ToolTip="{DynamicResource Design_LockItemsInOut}">
                            <ui:SymbolIcon Foreground="{Binding IsLocked, Converter={StaticResource LockColorConverter}}" Symbol="{Binding IsLocked, Converter={StaticResource LockSymbolConverter}}" />
                        </Button>

                        <!--  Visibility Checkbox  -->
                        <CheckBox
                            Grid.Column="2"
                            Margin="5,0,8,0"
                            VerticalAlignment="Center"
                            IsChecked="{Binding Hidden, Converter={StaticResource InverseBooleanConverter}}" />
                    </Grid>
                </Expander.Header>
                <!--  Child Items ListView  -->
                <ListView
                    Margin="0,-10,0,-10"
                    HorizontalContentAlignment="Stretch"
                    dd:DragDrop.DropHandler="{Binding RelativeSource={RelativeSource AncestorType=UserControl}}"
                    dd:DragDrop.IsDragSource="True"
                    dd:DragDrop.IsDropTarget="True"
                    dd:DragDrop.SelectDroppedItems="True"
                    dd:DragDrop.UseDefaultDragAdorner="True"
                    Background="Transparent"
                    BorderThickness="0"
                    ItemTemplate="{StaticResource SimpleItemTemplate_MarginB}"
                    ItemsSource="{Binding DisplayItems}"
                    PreviewMouseWheel="InnerListView_PreviewMouseWheel"
                    ScrollViewer.CanContentScroll="False"
                    ScrollViewer.VerticalScrollBarVisibility="Disabled"
                    SelectionChanged="ListViewGroupItems_SelectionChanged"
                    SelectionMode="Extended"
                    VirtualizingStackPanel.IsVirtualizing="False"
                    VirtualizingStackPanel.IsVirtualizingWhenGrouping="False">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Margin" Value="0,1,0,1" />
                            <Setter Property="FontSize" Value="12" />
                            <Setter Property="IsSelected" Value="{Binding Selected}" />
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
            </Expander>
        </DataTemplate>

        <local:PanelItemTemplateSelector
            x:Key="PanelItemsSelector"
            GroupTemplate="{StaticResource GroupItemTemplate}"
            SimpleTemplate="{StaticResource SimpleItemTemplate_MarginA}" />


    </UserControl.Resources>

    <Grid Margin="10,10,10,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ui:TextBox
                    Name="TextBoxSearch"
                    Grid.Column="0"
                    Width="200"
                    Margin="0,0,0,0"
                    PlaceholderText="{DynamicResource Common_SearchItems}"
                    TextChanged="TextBoxSearch_TextChanged" />
                <StackPanel
                    Grid.Column="2"
                    Margin="0,0,0,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    IsEnabled="{Binding Source={x:Static app:SharedModel.Instance}, Path=SelectedProfile.Active}"
                    Orientation="Horizontal">
                    <ToggleButton
                        Margin="10,0,0,0"
                        IsChecked="{Binding Source={x:Static app:SharedModel.Instance}, Path=SelectedProfile.OpenGL}"
                        ToolTip="OpenGL">
                        <ui:SymbolIcon Symbol="FastAcceleration20" />
                    </ToggleButton>

                    <ToggleButton
                        Margin="10,0,0,0"
                        IsChecked="{Binding Source={x:Static app:SharedModel.Instance}, Path=SelectedProfile.ShowFps}"
                        ToolTip="{DynamicResource Profile_ShowOverlay}">
                        <ui:SymbolIcon Symbol="Info20" />
                    </ToggleButton>

                    <ToggleButton
                        Margin="10,0,0,0"
                        IsChecked="{Binding Source={x:Static app:SharedModel.Instance}, Path=SelectedProfile.StrictWindowMatching}"
                        ToolTip="{DynamicResource Profile_StrictMatching}">
                        <ui:SymbolIcon Symbol="WindowInprivate20" />
                    </ToggleButton>

                    <ToggleButton
                        Margin="10,0,0,0"
                        IsChecked="{Binding Source={x:Static app:SharedModel.Instance}, Path=SelectedProfile.Topmost}"
                        ToolTip="{DynamicResource Profile_AlwaysOnTop}">
                        <ui:SymbolIcon Symbol="ShareScreenStart20" />
                    </ToggleButton>

                    <ToggleButton
                        Margin="10,0,0,0"
                        IsChecked="{Binding Source={x:Static app:SharedModel.Instance}, Path=SelectedProfile.Drag}"
                        ToolTip="{DynamicResource Profile_EnableDragMove}">
                        <ui:SymbolIcon Symbol="Drag20" />
                    </ToggleButton>

                    <ToggleButton
                        Margin="10,0,0,0"
                        IsChecked="{Binding Source={x:Static app:SharedModel.Instance}, Path=SelectedProfile.Resize}"
                        ToolTip="{DynamicResource Profile_EnableDragResize}">
                        <ui:SymbolIcon Symbol="Resize20" />
                    </ToggleButton>

                </StackPanel>
            </Grid>

            <Separator Grid.Row="2" Margin="0,5,0,0" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />

            </Grid.ColumnDefinitions>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <ListView
                    x:Name="ListViewItems"
                    Grid.Row="1"
                    Width="auto"
                    HorizontalContentAlignment="Stretch"
                    dd:DragDrop.DropHandler="{Binding}"
                    dd:DragDrop.IsDragSource="True"
                    dd:DragDrop.IsDropTarget="True"
                    dd:DragDrop.SelectDroppedItems="True"
                    dd:DragDrop.UseDefaultDragAdorner="True"
                    Background="Transparent"
                    ItemTemplateSelector="{StaticResource PanelItemsSelector}"
                    ItemsSource="{Binding Source={StaticResource DisplayItemsViewSource}}"
                    ScrollViewer.CanContentScroll="False"
                    ScrollViewer.VerticalScrollBarVisibility="Visible"
                    SelectionChanged="ListViewItems_SelectionChanged"
                    SelectionMode="Extended"
                    VirtualizingStackPanel.IsVirtualizing="False"
                    VirtualizingStackPanel.IsVirtualizingWhenGrouping="False">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Margin" Value="0,1,0,1" />
                            <Setter Property="IsSelected" Value="{Binding Selected}" />
                            <!--  Only apply the border style to GroupDisplayItem  -->
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Kind}" Value="Group">
                                    <Setter Property="Margin" Value="0,0,0,0" />
                                    <Setter Property="Background" Value="Transparent" />
                                    <Setter Property="BorderThickness" Value="1" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListViewItem">
                                                <Border
                                                    x:Name="Border"
                                                    Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}">
                                                    <ContentPresenter
                                                        Margin="2"
                                                        HorizontalAlignment="Stretch"
                                                        VerticalAlignment="Center" />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Border" Property="BorderThickness" Value="1" />
                                                        <Setter Property="BorderBrush" Value="LightSkyBlue" />
                                                    </Trigger>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="Border" Property="BorderThickness" Value="1" />
                                                        <Setter Property="BorderBrush" Value="DeepSkyBlue" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
            </Grid>

            <StackPanel
                Grid.Column="1"
                Margin="5,0,0,0"
                VerticalAlignment="Bottom">

                <ui:Button
                    x:Name="ButtonPushBack"
                    Margin="0,10,0,0"
                    Click="ButtonPushBack_Click"
                    Content=""
                    Icon="{ui:SymbolIcon ArrowUpload20}"
                    IsEnabled="{Binding Source={x:Static app:SharedModel.Instance}, Path=IsSingleItemSelected}"
                    ToolTip="{DynamicResource Design_PushItemTop}" />
                <ui:Button
                    x:Name="ButtonPushUp"
                    Margin="0,10,0,0"
                    Click="ButtonPushUp_Click"
                    Content=""
                    Icon="{ui:SymbolIcon ArrowUp20}"
                    IsEnabled="{Binding Source={x:Static app:SharedModel.Instance}, Path=IsSingleItemSelected}"
                    ToolTip="{DynamicResource Design_PushItemUp}" />
                <ui:Button
                    x:Name="ButtonPushDown"
                    Margin="0,10,0,0"
                    Click="ButtonPushDown_Click"
                    Content=""
                    Icon="{ui:SymbolIcon ArrowDown20}"
                    IsEnabled="{Binding Source={x:Static app:SharedModel.Instance}, Path=IsSingleItemSelected}"
                    ToolTip="{DynamicResource Design_BringItemDown}" />
                <ui:Button
                    x:Name="ButtonPushFront"
                    Margin="0,10,0,0"
                    Click="ButtonPushFront_Click"
                    Content=""
                    Icon="{ui:SymbolIcon ArrowDownload20}"
                    IsEnabled="{Binding Source={x:Static app:SharedModel.Instance}, Path=IsSingleItemSelected}"
                    ToolTip="{DynamicResource Design_BringItemBottom}" />
                <ui:Button
                    x:Name="ButtonDuplicate"
                    Margin="0,10,0,0"
                    Click="ButtonDuplicate_Click"
                    Content=""
                    Icon="{ui:SymbolIcon Copy20}"
                    IsEnabled="{Binding Source={x:Static app:SharedModel.Instance}, Path=IsSingleItemSelected}"
                    ToolTip="{DynamicResource Design_DuplicateSelectedItem}" />
                <ui:Button
                    x:Name="ButtonDelete"
                    Margin="0,10,0,0"
                    Appearance="Danger"
                    Click="ButtonDelete_Click"
                    Content=""
                    Icon="{ui:SymbolIcon Delete20}"
                    IsEnabled="{Binding Source={x:Static app:SharedModel.Instance}, Path=IsSingleItemSelected}"
                    ToolTip="{DynamicResource Design_DeleteSelectedItem}" />
            </StackPanel>

        </Grid>

        <Separator Grid.Row="2" Margin="0,5,0,0" />

        <Grid Grid.Row="4" Margin="0,8,0,8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <components:CommonActions Grid.Row="0" HorizontalAlignment="Left" />

            <StackPanel
                Grid.Row="1"
                Margin="0,6,0,0"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <StackPanel
                    MinWidth="28"
                    Margin="0,0,10,0"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Visibility="{Binding Source={x:Static app:ConfigModel.Instance}, Path=Settings.AutosaveEnabled, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ui:SymbolIcon
                        x:Name="AutosavePendingIcon"
                        Width="20"
                        Height="20"
                        VerticalAlignment="Center"
                        RenderTransformOrigin="0.5,0.5"
                        Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                        Symbol="ArrowSync20"
                        ToolTip="{DynamicResource Design_AutosavePending}"
                        Visibility="{Binding Source={x:Static app:ConfigModel.Instance}, Path=AutosaveIndicator, Converter={StaticResource EnumEqualsConverter}, ConverterParameter=Pending}">
                        <ui:SymbolIcon.RenderTransform>
                            <RotateTransform Angle="0" />
                        </ui:SymbolIcon.RenderTransform>
                        <ui:SymbolIcon.Style>
                            <Style TargetType="ui:SymbolIcon">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Source={x:Static app:ConfigModel.Instance}, Path=AutosaveIndicator}" Value="Pending">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard x:Name="SpinStoryboard">
                                                <Storyboard RepeatBehavior="Forever">
                                                    <DoubleAnimation
                                                        Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                        From="0"
                                                        To="360"
                                                        Duration="0:0:1" />
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.EnterActions>
                                        <DataTrigger.ExitActions>
                                            <StopStoryboard BeginStoryboardName="SpinStoryboard" />
                                        </DataTrigger.ExitActions>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ui:SymbolIcon.Style>
                    </ui:SymbolIcon>
                    <ui:SymbolIcon
                        x:Name="AutosaveDoneIcon"
                        Width="20"
                        Height="20"
                        Margin="-20,0,0,0"
                        VerticalAlignment="Center"
                        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                        Symbol="Checkmark20"
                        ToolTip="{DynamicResource Design_AutoSaved}"
                        Visibility="{Binding Source={x:Static app:ConfigModel.Instance}, Path=AutosaveIndicator, Converter={StaticResource EnumEqualsConverter}, ConverterParameter=AutoSaved}">
                        <ui:SymbolIcon.Style>
                            <Style TargetType="ui:SymbolIcon">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Source={x:Static app:ConfigModel.Instance}, Path=AutosaveIndicator}" Value="AutoSaved">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation
                                                        Storyboard.TargetProperty="Opacity"
                                                        From="0.0"
                                                        To="1.0"
                                                        Duration="0:0:0.3" />
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.EnterActions>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ui:SymbolIcon.Style>
                    </ui:SymbolIcon>
                    <TextBlock
                        Margin="8,0,0,0"
                        VerticalAlignment="Center"
                        FontSize="12"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="{Binding Source={x:Static app:ConfigModel.Instance}, Path=LastSaveText}"
                        Visibility="{Binding Source={x:Static app:ConfigModel.Instance}, Path=LastSaveAt, Converter={StaticResource NullToVisibilityConverter}}" />
                </StackPanel>
                <ui:Button
                    x:Name="ButtonSave"
                    Width="105"
                    Margin="0,0,0,0"
                    Appearance="Primary"
                    Click="ButtonSave_Click"
                    Content="{DynamicResource Common_Save}"
                    ToolTip="{DynamicResource Profile_MakePermanent}" />
                <ui:Button
                    x:Name="ButtonUndo"
                    Margin="10,0,0,0"
                    Click="ButtonUndo_Click"
                    Content=""
                    Icon="{ui:SymbolIcon ArrowUndo20}"
                    IsEnabled="{Binding Source={x:Static app:SharedModel.Instance}, Path=CanUndo}"
                    ToolTip="{DynamicResource Common_Undo}" />
                <ui:Button
                    x:Name="ButtonRedo"
                    Margin="5,0,0,0"
                    Click="ButtonRedo_Click"
                    Content=""
                    Icon="{ui:SymbolIcon ArrowRedo20}"
                    IsEnabled="{Binding Source={x:Static app:SharedModel.Instance}, Path=CanRedo}"
                    ToolTip="{DynamicResource Common_Redo}" />
                <ui:Button
                    x:Name="ButtonReload"
                    Width="105"
                    Margin="5,0,0,0"
                    Appearance="Danger"
                    Click="ButtonReload_Click"
                    Content="{DynamicResource Common_Reload}"
                    ToolTip="{DynamicResource Profile_DiscardChanges}" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
