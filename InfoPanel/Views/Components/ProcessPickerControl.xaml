<UserControl
    x:Class="InfoPanel.Views.Components.ProcessPickerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Width="400"
    MinHeight="400"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,10"
            FontSize="12"
            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
            Text="{DynamicResource Prop_SelectRunningProcess}"
            TextWrapping="Wrap" />

        <ui:TextBox
            Grid.Row="1"
            x:Name="FilterTextBox"
            Margin="0,0,0,10"
            PlaceholderText="{DynamicResource Common_FilterDots}"
            TextChanged="FilterTextBox_TextChanged">
            <ui:TextBox.Icon>
                <ui:SymbolIcon Symbol="Search20" />
            </ui:TextBox.Icon>
        </ui:TextBox>

        <ui:Card Grid.Row="2" Padding="0">
            <ListView
                x:Name="ProcessListView"
                BorderThickness="0"
                Background="Transparent"
                MouseDoubleClick="ProcessListView_MouseDoubleClick"
                SelectionChanged="ProcessListView_SelectionChanged"
                SelectionMode="Single">
                <ListView.View>
                    <GridView>
                        <GridViewColumn
                            Width="360"
                            DisplayMemberBinding="{Binding ProcessName}"
                            Header="{DynamicResource Prop_ProcessName}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </ui:Card>
    </Grid>
</UserControl>
