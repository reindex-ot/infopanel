<UserControl
    x:Class="InfoPanel.Views.Components.PluginProperties"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:InfoPanel.Views.Components"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:converters="clr-namespace:InfoPanel.Views.Converters"
    xmlns:viewmodels="clr-namespace:InfoPanel.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:PluginViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converters:PluginConfigTemplateSelector x:Key="PluginConfigTemplateSelector">
            <converters:PluginConfigTemplateSelector.StringTemplate>
                <DataTemplate DataType="{x:Type viewmodels:PluginConfigPropertyViewModel}">
                    <Grid Margin="0,6,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            VerticalAlignment="Center"
                            FontSize="12"
                            Text="{Binding DisplayName, Converter={StaticResource LocalizedTextConverter}}"
                            ToolTip="{Binding Description, TargetNullValue={x:Null}}" />
                        <TextBox
                            Grid.Column="1"
                            FontSize="12"
                            Text="{Binding StringValue, UpdateSourceTrigger=LostFocus}" />
                    </Grid>
                </DataTemplate>
            </converters:PluginConfigTemplateSelector.StringTemplate>
            <converters:PluginConfigTemplateSelector.IntegerTemplate>
                <DataTemplate DataType="{x:Type viewmodels:PluginConfigPropertyViewModel}">
                    <Grid Margin="0,6,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            VerticalAlignment="Center"
                            FontSize="12"
                            Text="{Binding DisplayName, Converter={StaticResource LocalizedTextConverter}}"
                            ToolTip="{Binding Description, TargetNullValue={x:Null}}" />
                        <ui:NumberBox
                            Grid.Column="1"
                            FontSize="12"
                            MaxDecimalPlaces="0"
                            Minimum="{Binding MinValue}"
                            Maximum="{Binding MaxValue}"
                            SmallChange="{Binding Step, TargetNullValue=1}"
                            Text="{Binding NumericValue}"
                            TextChanged="NumberBox_TextChanged"
                            Value="{Binding NumericValue, Mode=OneWay}" />
                    </Grid>
                </DataTemplate>
            </converters:PluginConfigTemplateSelector.IntegerTemplate>
            <converters:PluginConfigTemplateSelector.DoubleTemplate>
                <DataTemplate DataType="{x:Type viewmodels:PluginConfigPropertyViewModel}">
                    <Grid Margin="0,6,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            VerticalAlignment="Center"
                            FontSize="12"
                            Text="{Binding DisplayName}"
                            ToolTip="{Binding Description, TargetNullValue={x:Null}}" />
                        <ui:NumberBox
                            Grid.Column="1"
                            FontSize="12"
                            MaxDecimalPlaces="2"
                            Minimum="{Binding MinValue}"
                            Maximum="{Binding MaxValue}"
                            SmallChange="{Binding Step, TargetNullValue=0.1}"
                            Text="{Binding NumericValue}"
                            TextChanged="NumberBox_TextChanged"
                            Value="{Binding NumericValue, Mode=OneWay}" />
                    </Grid>
                </DataTemplate>
            </converters:PluginConfigTemplateSelector.DoubleTemplate>
            <converters:PluginConfigTemplateSelector.BooleanTemplate>
                <DataTemplate DataType="{x:Type viewmodels:PluginConfigPropertyViewModel}">
                    <Grid Margin="0,6,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            VerticalAlignment="Center"
                            FontSize="12"
                            Text="{Binding DisplayName}"
                            ToolTip="{Binding Description, TargetNullValue={x:Null}}" />
                        <ui:ToggleSwitch
                            Grid.Column="1"
                            IsChecked="{Binding BoolValue}" />
                    </Grid>
                </DataTemplate>
            </converters:PluginConfigTemplateSelector.BooleanTemplate>
            <converters:PluginConfigTemplateSelector.ChoiceTemplate>
                <DataTemplate DataType="{x:Type viewmodels:PluginConfigPropertyViewModel}">
                    <Grid Margin="0,6,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            VerticalAlignment="Center"
                            FontSize="12"
                            Text="{Binding DisplayName}"
                            ToolTip="{Binding Description, TargetNullValue={x:Null}}" />
                        <ComboBox
                            Grid.Column="1"
                            FontSize="12"
                            ItemsSource="{Binding Options}"
                            SelectedIndex="{Binding SelectedIndex}" />
                    </Grid>
                </DataTemplate>
            </converters:PluginConfigTemplateSelector.ChoiceTemplate>
        </converters:PluginConfigTemplateSelector>
    </UserControl.Resources>
    <Grid>
        <ui:CardExpander Margin="0,10,0,0" IsEnabled="{Binding ControlEnabled}">
            <ui:CardExpander.Header>
                <StackPanel>
                    <Grid Margin="0,0,20,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="auto" />
                            <ColumnDefinition Width="auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Margin="0,0,20,0">

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto" />
                                    <ColumnDefinition Width="auto" />
                                    <ColumnDefinition Width="auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock VerticalAlignment="Center" Text="{Binding Name}" />

                                <TextBlock
                                    Grid.Column="2"
                                    VerticalAlignment="Center"
                                    FontSize="12"
                                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                    Text="{Binding Author, StringFormat=' by {0}'}"
                                    Visibility="{Binding Author, Converter={StaticResource NullToVisibilityConverter}}" />

                                <TextBlock
                                    Grid.Column="1"
                                    VerticalAlignment="Center"
                                    FontSize="12"
                                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                    Text="{Binding Version, StringFormat=' v{0}'}"
                                    Visibility="{Binding Version, Converter={StaticResource NullToVisibilityConverter}}" />

                            </Grid>



                            <TextBlock
                                Margin="0,5,0,0"
                                VerticalAlignment="Center"
                                FontSize="12"
                                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                Text="{Binding Description}"
                                TextWrapping="Wrap" />

                            <ui:HyperlinkButton
                                Margin="0,5,0,0"
                                Content="{Binding Website}"
                                FontSize="12"
                                NavigateUri="{Binding Website}"
                                Visibility="{Binding Website, Converter={StaticResource NullToVisibilityConverter}}">
                                <ui:HyperlinkButton.Icon>
                                    <ui:SymbolIcon Symbol="Link20" />
                                </ui:HyperlinkButton.Icon>
                            </ui:HyperlinkButton>
                        </StackPanel>


                        <StackPanel
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            Margin="0,0,20,0"
                            Visibility="{Binding ShowMetrics, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Border
                                Width="120"
                                Background="{DynamicResource ControlFillColorDefaultBrush}"
                                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="4"
                                Padding="8,4">
                                <StackPanel Orientation="Horizontal">
                                    <ui:SymbolIcon
                                        FontSize="12"
                                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                        Symbol="TopSpeed20" />
                                    <TextBlock
                                        Margin="5,0,0,0"
                                        VerticalAlignment="Center"
                                        FontSize="11"
                                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                        Text="CPU" />
                                    <TextBlock
                                        Margin="6,0,0,0"
                                        VerticalAlignment="Center"
                                        FontSize="11"
                                        FontWeight="Medium"
                                        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                        Text="{Binding CpuUsage, StringFormat='{}{0:F1}%'}" />
                                </StackPanel>
                            </Border>
                            <Border
                                Width="120"
                                Margin="0,4,0,0"
                                Background="{DynamicResource ControlFillColorDefaultBrush}"
                                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="4"
                                Padding="8,4">
                                <StackPanel Orientation="Horizontal">
                                    <ui:SymbolIcon
                                        FontSize="12"
                                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                        Symbol="Memory16" />
                                    <TextBlock
                                        Margin="5,0,0,0"
                                        VerticalAlignment="Center"
                                        FontSize="11"
                                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                        Text="MEM" />
                                    <TextBlock
                                        Margin="6,0,0,0"
                                        VerticalAlignment="Center"
                                        FontSize="11"
                                        FontWeight="Medium"
                                        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                        Text="{Binding MemoryUsage}" />
                                </StackPanel>
                            </Border>
                        </StackPanel>

                        <Grid Grid.Column="2" Margin="0,0,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <ui:ToggleSwitch
                                Margin="0,0,0,0"
                                VerticalAlignment="Center"
                                IsChecked="{Binding Activated}" />
                            <ui:Button
                                Grid.Row="1"
                                Width="32"
                                Height="32"
                                Margin="0,10,0,0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Bottom"
                                Appearance="Danger"
                                BorderThickness="0"
                                Command="{Binding RemoveCommand}"
                                Icon="{ui:SymbolIcon Delete20}"
                                ToolTip="{DynamicResource Plugins_Uninstall}"
                                Visibility="{Binding ShowRemoveButton, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        </Grid>
                    </Grid>


                </StackPanel>
            </ui:CardExpander.Header>

            <StackPanel>
            <TextBlock
                Margin="0,0,0,0"
                FontSize="12"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                Text="{DynamicResource Plugins_NotLoaded}"
                Visibility="{Binding Activated, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />

            <ItemsControl
                Margin="0,0,0,0"
                d:ItemsSource="{Binding Plugins}"
                ItemsSource="{Binding Plugins}"
                Visibility="{Binding Activated, Converter={StaticResource BooleanToVisibilityConverter}}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ui:CardExpander Margin="0,10,0,0" IsExpanded="{Binding HasConfigProperties, Mode=OneTime}">
                            <ui:CardExpander.Header>
                                <Grid Margin="0,0,20,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock
                                            FontSize="13"
                                            FontWeight="Medium"
                                            Text="{Binding Name, Converter={StaticResource LocalizedTextConverter}}" />

                                        <TextBlock
                                            Margin="0,5,0,0"
                                            VerticalAlignment="Center"
                                            FontSize="12"
                                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                            Text="{Binding Description, Converter={StaticResource LocalizedTextConverter}}" />

                                        <ItemsControl
                                            Margin="0,10,0,0"
                                            ItemsSource="{Binding Actions}"
                                            Visibility="{Binding Actions, Converter={StaticResource NullToVisibilityConverter}}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal" />
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button
                                                        Margin="0,0,10,0"
                                                        Command="{Binding Command}"
                                                        Content="{Binding DisplayName, Converter={StaticResource LocalizedTextConverter}}" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <ui:Anchor
                                            Width="150"
                                            VerticalAlignment="Center"
                                            Appearance="Secondary"
                                            Content="{DynamicResource Plugins_OpenConfigFile}"
                                            NavigateUri="{Binding ConfigFilePath}"
                                            Visibility="{Binding ConfigFilePath, Converter={StaticResource NullToVisibilityConverter}}" />

                                        <Button
                                            Width="150"
                                            Margin="0,10,0,0"
                                            Command="{Binding ReloadCommand}"
                                            Content="{DynamicResource Common_Reload}"
                                            Visibility="{Binding ConfigFilePath, Converter={StaticResource NullToVisibilityConverter}}" />
                                    </StackPanel>
                                </Grid>
                            </ui:CardExpander.Header>

                            <ItemsControl
                                Margin="0,0,0,0"
                                IsEnabled="{Binding ConfigLoaded}"
                                ItemsSource="{Binding ConfigProperties}"
                                ItemTemplateSelector="{StaticResource PluginConfigTemplateSelector}" />

                        </ui:CardExpander>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            </StackPanel>

        </ui:CardExpander>
    </Grid>
</UserControl>
