<UserControl x:Class="InfoPanel.Views.Components.Menu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InfoPanel.Views.Components"
             xmlns:app="clr-namespace:InfoPanel"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!--<materialDesign:Card>-->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Menu Grid.Column="0">
                <MenuItem Header="File" HorizontalAlignment="Left">
                    <!--<MenuItem Header="{DynamicResource Profile_ExportProfile}"/>
                <MenuItem Header="{DynamicResource Profile_ImportProfileTooltip}"/>-->
                    <MenuItem Name="MenuItemExit" Header="{DynamicResource Menu_ExitProgram}" Click="MenuItemExit_Click"/>
                </MenuItem>
                <MenuItem Header="{DynamicResource Nav_Settings}">
                    <CheckBox Content="{DynamicResource Menu_StartWithWindows}" IsChecked="{Binding Source={x:Static app:ConfigModel.Instance}, Path=Settings.AutoStart}" Margin="0,0,0,0"/>
                    <CheckBox Content="{DynamicResource Menu_StartMinimized}" IsChecked="{Binding Source={x:Static app:ConfigModel.Instance}, Path=Settings.StartMinimized}"/>
                    <CheckBox Content="{DynamicResource Menu_BeadaPanelSupport}" IsChecked="{Binding Source={x:Static app:ConfigModel.Instance}, Path=Settings.Beadapanel}"/>
                    <CheckBox Grid.Column="0" Content="{DynamicResource Menu_WebServerSupport}" IsChecked="{Binding Source={x:Static app:ConfigModel.Instance}, Path=Settings.WebServer}"/>
                </MenuItem>
                <MenuItem Header="{DynamicResource Common_Misc}">
                    <MenuItem Name="MenuItemPerformanceSettings" Header="{DynamicResource Common_PerformanceSettings}" Click="MenuItemPerformanceSettings_Click"/>
                    <MenuItem Name="MenuItemWebserverSettings" Header="{DynamicResource Menu_WebServerSettings}" Click="MenuItemWebserverSettings_Click"/>
                </MenuItem>
                <MenuItem Header="Help">
                    <MenuItem Name="MenuItemDiscord" Header="Discord" Click="MenuItemDiscord_Click"/>
                </MenuItem>
            </Menu>
            <TextBlock Grid.Column="2" VerticalAlignment="Center" Margin="0,0,10,0">
                <Span FontWeight="Bold" FontSize="24">infopanel</Span>
            </TextBlock>
            <!--<Image x:Name="ImageLogo" Grid.Column="2" Source="{StaticResource Logo}" 
                       Cursor="Hand" ToolTip="https://infopanel.pages.dev"
                       Height="30" Margin="0,0,10,0"/>-->
        </Grid>
    <!--</materialDesign:Card>-->
</UserControl>
