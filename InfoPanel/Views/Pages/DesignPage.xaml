<Page
    x:Class="InfoPanel.Views.Pages.DesignPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:components="clr-namespace:InfoPanel.Views.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:InfoPanel"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:InfoPanel.Models"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="{DynamicResource Nav_Design}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    DataContext="{Binding RelativeSource={RelativeSource self}}"
    ScrollViewer.CanContentScroll="False"
    mc:Ignorable="d">

    <Grid Margin="20,-70,20,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Border
                x:Name="SensorAreaBorder"
                Margin="0,60,580,0"
                CornerRadius="8">
                <Grid Margin="0,0,0,0">
                    <TabControl Margin="0,10,0,0" TabStripPlacement="Top">
                        <TabItem Width="140">
                            <TabItem.Header>
                                <StackPanel
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Orientation="Horizontal">
                                    <Image
                                        Width="30"
                                        Height="30"
                                        Source="/InfoPanel;component/Resources/Images/hwinfo_icon.ico" />
                                    <TextBlock
                                        Grid.Column="0"
                                        Margin="10,0,0,0"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center">
                                        <Bold FontSize="16" FontWeight="SemiBold">HWiNFO</Bold>
                                    </TextBlock>
                                </StackPanel>
                            </TabItem.Header>
                            <Grid>
                                <StackPanel
                                    Margin="10,10,10,10"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center">
                                    <Image
                                        Width="300"
                                        Height="80"
                                        Source="/InfoPanel;component/Resources/Images/hwinfo_logo_white.png" />
                                    <ui:HyperlinkButton
                                        Margin="0,10,0,0"
                                        HorizontalAlignment="Center"
                                        Content="https://www.hwinfo.com/download/"
                                        FontSize="14"
                                        NavigateUri="https://www.hwinfo.com/download/" />
                                    <TextBlock
                                        Margin="50,20,50,0"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center"
                                        FontSize="14"
                                        TextAlignment="Justify"
                                        TextWrapping="Wrap">
                                        <Run Text="{DynamicResource Design_HwInfoSHMError}" /><LineBreak /><LineBreak />
                                        <Run Text="{DynamicResource Design_HwInfoSettingsEnsure}" /><LineBreak /><LineBreak />
                                        <Run Text="{DynamicResource Design_HwInfoSetting1}" /><LineBreak />
                                        <Run Text="{DynamicResource Design_HwInfoSetting2}" /></TextBlock>
                                </StackPanel>
                                <Border
                                    Background="{DynamicResource SolidBackgroundFillColorBaseBrush}"
                                    CornerRadius="0,4,4,4"
                                    Visibility="{Binding Source={x:Static local:SharedModel.Instance}, Path=HwInfoAvailable, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <components:HwInfoSensors Margin="0,0,0,10" />
                                </Border>
                            </Grid>
                        </TabItem>
                        <TabItem Width="140">
                            <TabItem.Header>
                                <StackPanel
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Orientation="Horizontal">
                                    <Image
                                        Width="30"
                                        Height="30"
                                        Source="/InfoPanel;component/Resources/Images/libre_icon.ico" />
                                    <TextBlock
                                        Grid.Column="0"
                                        Margin="10,0,0,0"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center">
                                        <Bold FontSize="16" FontWeight="SemiBold">Libre</Bold>
                                    </TextBlock>
                                </StackPanel>
                            </TabItem.Header>
                            <Grid>
                                <StackPanel
                                    Margin="10,10,10,10"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center">
                                    <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                                        <Image Height="80" Source="/InfoPanel;component/Resources/Images/libre_icon.png" />
                                        <TextBlock
                                            Margin="10,0,0,0"
                                            VerticalAlignment="Center"
                                            FontSize="24">
                                            LibreHardwareMonitor
                                        </TextBlock>
                                    </StackPanel>
                                    <ui:HyperlinkButton
                                        Margin="0,10,0,0"
                                        HorizontalAlignment="Center"
                                        Content="https://github.com/LibreHardwareMonitor/LibreHardwareMonitor"
                                        FontSize="14"
                                        NavigateUri="https://github.com/LibreHardwareMonitor/LibreHardwareMonitor" />
                                    <TextBlock
                                        Margin="50,20,50,0"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center"
                                        FontSize="14"
                                        TextAlignment="Justify"
                                        TextWrapping="Wrap"
                                        Text="{DynamicResource Design_LibreDisabledDesc}" />
                                    <Button
                                        Width="150"
                                        Margin="0,50,0,0"
                                        HorizontalAlignment="Center"
                                        Command="{Binding ViewModel.NavigateCommand}"
                                        CommandParameter="navigate_to_settings"
                                        Content="{DynamicResource Nav_Settings}" />
                                </StackPanel>
                                <Border
                                    Background="{DynamicResource SolidBackgroundFillColorBaseBrush}"
                                    CornerRadius="0,4,4,4"
                                    Visibility="{Binding Source={x:Static local:ConfigModel.Instance}, Path=Settings.LibreHardwareMonitor, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <components:LibreSensors Margin="0,0,0,10" />
                                </Border>
                            </Grid>
                        </TabItem>
                        <TabItem>
                            <TabItem.Header>
                                <StackPanel
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Orientation="Horizontal">
                                    <Image
                                        Width="30"
                                        Height="30"
                                        Source="/InfoPanel;component/Resources/Images/logo.ico" />
                                    <TextBlock
                                        Grid.Column="0"
                                        Margin="10,0,0,0"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center">
                                        <Bold FontSize="16" FontWeight="SemiBold">Plugins</Bold>
                                    </TextBlock>
                                </StackPanel>
                            </TabItem.Header>
                            <Grid>
                                <Border Background="{DynamicResource SolidBackgroundFillColorBaseBrush}" CornerRadius="0,4,4,4">
                                    <components:PluginSensors Margin="0,0,0,10" />
                                </Border>
                            </Grid>
                        </TabItem>
                    </TabControl>


                </Grid>
            </Border>

            <!--  Toggle button to reopen items panel, overlaid at top-right of sensor area  -->
            <Button
                x:Name="ShowItemsPanelButton"
                Margin="0,70,5,0"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Panel.ZIndex="5"
                Click="ShowItemsPanel_Click"
                ToolTip="{DynamicResource Design_ShowDisplayItems}"
                Visibility="Collapsed">
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="PanelRight24" />
                    <TextBlock Margin="6,0,0,0" Text="{DynamicResource Common_Items}" />
                </StackPanel>
            </Button>

            <!--  Flyout overlay panel for Display Items  -->
            <Border
                x:Name="ItemsPanelOverlay"
                Width="570"
                Margin="0,70,0,0"
                HorizontalAlignment="Right"
                VerticalAlignment="Stretch"
                Panel.ZIndex="10"
                Background="{DynamicResource SolidBackgroundFillColorBaseBrush}"
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1,0,0,0"
                CornerRadius="8,0,0,8">
                <Border.Effect>
                    <DropShadowEffect
                        BlurRadius="20"
                        Direction="180"
                        Opacity="0.3"
                        ShadowDepth="2"
                        Color="Black" />
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="10,10,10,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ComboBox
                            Name="ComboBoxSelectedProfile"
                            Grid.Column="0"
                            Width="220"
                            DisplayMemberPath="Name"
                            ItemsSource="{Binding Source={x:Static local:ConfigModel.Instance}, Path=Profiles}"
                            SelectedItem="{Binding Source={x:Static local:SharedModel.Instance}, Path=SelectedProfile}" />

                        <StackPanel
                            Grid.Column="1"
                            Margin="10,0,0,0"
                            VerticalAlignment="Center">
                            <TextBlock FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}W: {0}, H: {1}">
                                        <Binding
                                            Mode="OneWay"
                                            Path="SelectedProfile.Width"
                                            Source="{x:Static local:SharedModel.Instance}" />
                                        <Binding
                                            Mode="OneWay"
                                            Path="SelectedProfile.Height"
                                            Source="{x:Static local:SharedModel.Instance}" />
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                            <TextBlock FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}X: {0}, Y: {1}">
                                        <Binding
                                            Mode="OneWay"
                                            Path="SelectedProfile.WindowX"
                                            Source="{x:Static local:SharedModel.Instance}" />
                                        <Binding
                                            Mode="OneWay"
                                            Path="SelectedProfile.WindowY"
                                            Source="{x:Static local:SharedModel.Instance}" />
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </StackPanel>

                        <ui:ToggleSwitch
                            Grid.Column="3"
                            Margin="5,0,5,0"
                            VerticalAlignment="Center"
                            IsChecked="{Binding Source={x:Static local:SharedModel.Instance}, Path=SelectedProfile.Active}" />

                        <Button
                            Grid.Column="4"
                            Width="28"
                            Height="28"
                            Padding="0"
                            VerticalAlignment="Center"
                            Background="Transparent"
                            BorderThickness="0"
                            Click="HideItemsPanel_Click"
                            Cursor="Hand"
                            ToolTip="{DynamicResource Profile_ClosePanel}">
                            <ui:SymbolIcon FontSize="16" Symbol="Subtract24" />
                        </Button>
                    </Grid>

                    <Border
                        Grid.Row="1"
                        Margin="10,10,10,10"
                        Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="8">
                        <components:DisplayItems />
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <!--<Expander Grid.Row="1" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" MaxWidth="1000"
                  IsExpanded="{Binding Source={x:Static local:SharedModel.Instance}, Path=SelectedItem, Converter={StaticResource NullToBooleanConverter}, Mode=OneWay}"
             Margin="0,10,0,0">-->
        <Expander
            Grid.Row="1"
            Margin="0,10,0,10"
            Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
            IsExpanded="True">

            <Expander.Header>
                <Grid Margin="0,0,20,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>
                    <Label>
                        <TextBlock FontWeight="Medium" Text="{Binding Source={x:Static local:SharedModel.Instance}, Path=SelectedItem, Converter={StaticResource SelectedItemTextConverter}}" />
                    </Label>
                    <Button
                        Grid.Column="1"
                        Width="20"
                        Height="20"
                        Margin="10,0,-10,0"
                        Padding="0"
                        VerticalAlignment="Center"
                        Background="Transparent"
                        BorderThickness="0"
                        Command="{Binding UnselectCommand}"
                        Cursor="Hand"
                        Visibility="{Binding Source={x:Static local:SharedModel.Instance}, Path=SelectedItem, Converter={StaticResource NullToVisibilityConverter}}">
                        <ui:SymbolIcon Symbol="Dismiss24" />
                    </Button>
                </Grid>

            </Expander.Header>

            <ScrollViewer
                MaxHeight="350"
                HorizontalScrollBarVisibility="Disabled"
                VerticalScrollBarVisibility="Auto">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>

                    <ContentControl
                        Grid.Column="0"
                        Margin="0,0,0,0"
                        Content="{Binding Source={x:Static local:SharedModel.Instance}, Path=SelectedItem}">
                        <ContentControl.Style>
                            <Style TargetType="ContentControl">
                                <Setter Property="Content" Value="{Binding Source={x:Static local:SharedModel.Instance}, Path=SelectedItem}" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding RelativeSource={x:Static RelativeSource.Self}, Path=Content}" Value="{x:Null}">
                                        <Setter Property="ContentControl.Template">
                                            <Setter.Value>
                                                <ControlTemplate>
                                                    <Grid Margin="10,0,10,10">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition />
                                                            <ColumnDefinition />
                                                            <ColumnDefinition />
                                                        </Grid.ColumnDefinitions>
                                                         <TextBlock
                                                             Grid.Column="0"
                                                             Margin="0,0,0,0"
                                                             VerticalAlignment="Stretch"
                                                             AllowDrop="False"
                                                             TextWrapping="Wrap">
                                                             <Bold><Run Text="{DynamicResource Design_QuickGuideTitle}" /></Bold>
                                                             <LineBreak /><LineBreak />
                                                             <Bold>1.</Bold>
                                                             <Run Text="{DynamicResource Design_QuickGuide1}" /><LineBreak /><LineBreak />
                                                             <Bold>2.</Bold>
                                                             <Run Text="{DynamicResource Design_QuickGuide2}" /><LineBreak /><LineBreak />
                                                             <Bold>3.</Bold>
                                                             <Run Text="{DynamicResource Design_QuickGuide3}" /><LineBreak /><LineBreak />
                                                             <Bold>4.</Bold>
                                                             <Run Text="{DynamicResource Design_QuickGuide4}" /></TextBlock>
                                                         <TextBlock
                                                             Grid.Column="1"
                                                             Margin="10,0,0,0"
                                                             VerticalAlignment="Stretch"
                                                             AllowDrop="False"
                                                             TextWrapping="Wrap">
                                                             <Bold><Run Text="{DynamicResource Design_TipsTitle}" /></Bold>
                                                             <LineBreak /><LineBreak />
                                                             <Bold>1.</Bold>
                                                             <Run Text="{DynamicResource Design_Tips1}" /><LineBreak /><LineBreak />
                                                             <Bold>2.</Bold> <Bold Foreground="Orange"><Run Text="{DynamicResource Design_Tips2}" /></Bold>
                                                             <LineBreak /><LineBreak />
                                                             <Bold>3.</Bold> <Bold Foreground="IndianRed"><Run Text="{DynamicResource Design_Tips3}" /></Bold>
                                                             <LineBreak /><LineBreak />
                                                             <Bold>4.</Bold>
                                                             <Run Text="{DynamicResource Design_Tips4}" /><LineBreak /><LineBreak />
                                                             <Bold>5.</Bold>
                                                             <Run Text="{DynamicResource Design_Tips5}" /></TextBlock>
                                                         <TextBlock
                                                             Grid.Column="2"
                                                             Margin="10,0,0,0"
                                                             VerticalAlignment="Stretch"
                                                             AllowDrop="False"
                                                             TextWrapping="Wrap">
                                                             <Bold><Run Text="{DynamicResource Guide_AdditionalNotesTitle}" /></Bold>
                                                             <LineBreak /><LineBreak />
                                                             <Bold>1.</Bold>
                                                             <Run Text="{DynamicResource Design_AdditionalNote1}" />
                                                             <LineBreak /><LineBreak />
                                                             <Bold>2.</Bold>
                                                             <Run Text="{DynamicResource Design_AdditionalNote2}" />
                                                             <LineBreak /><LineBreak />
                                                             <Bold>3.</Bold>
                                                             <Run Text="{DynamicResource Design_AdditionalNote3}" /><LineBreak />
                                                         </TextBlock>
                                                    </Grid>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ContentControl.Style>
                        <ContentControl.Resources>
                            <DataTemplate DataType="{x:Type models:GroupDisplayItem}">
                                <components:GroupProperties GroupDisplayItem="{Binding}" />
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type models:GaugeDisplayItem}">
                                <components:CustomProperties GaugeDisplayItem="{Binding}" />
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type models:SensorDisplayItem}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <components:TextProperties TextDisplayItem="{Binding}" />
                                    <components:SensorProperties Grid.Column="1" SensorDisplayItem="{Binding}" />
                                </Grid>
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type models:TableSensorDisplayItem}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <components:TextProperties TextDisplayItem="{Binding}" />
                                    <components:TableSensorProperties Grid.Column="1" TableSensorDisplayItem="{Binding}" />
                                </Grid>
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type models:ClockDisplayItem}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <components:TextProperties TextDisplayItem="{Binding}" />
                                    <components:DateTimeProperties Grid.Column="1" />
                                </Grid>
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type models:CalendarDisplayItem}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <components:TextProperties TextDisplayItem="{Binding}" />
                                    <components:DateTimeProperties Grid.Column="1" />
                                </Grid>
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type models:TextDisplayItem}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <components:TextProperties TextDisplayItem="{Binding}" />
                                </Grid>
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type models:ImageDisplayItem}">
                                <components:ImageProperties />
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type models:GraphDisplayItem}">
                                <StackPanel Orientation="Vertical">
                                    <components:GraphProperties GraphDisplayItem="{Binding}" />
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type models:BarDisplayItem}">
                                <StackPanel Orientation="Vertical">
                                    <components:BarProperties BarDisplayItem="{Binding}" />
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type models:DonutDisplayItem}">
                                <StackPanel Orientation="Vertical">
                                    <components:DonutProperties DonutDisplayItem="{Binding}" />
                                </StackPanel>
                            </DataTemplate>
                            <DataTemplate DataType="{x:Type models:ShapeDisplayItem}">
                                <StackPanel Orientation="Vertical">
                                    <components:ShapeProperties ShapeDisplayItem="{Binding}" />
                                </StackPanel>
                            </DataTemplate>
                        </ContentControl.Resources>
                    </ContentControl>

                    <StackPanel Grid.Column="1">
                        <components:CommonProperties Margin="10,10,10,10" HorizontalAlignment="Center" />
                    </StackPanel>
                </Grid>
            </ScrollViewer>
        </Expander>
    </Grid>
</Page>
