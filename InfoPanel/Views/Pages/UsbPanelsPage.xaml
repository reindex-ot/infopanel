<Page
    x:Class="InfoPanel.Views.Pages.UsbPanelsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:components="clr-namespace:InfoPanel.Views.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:InfoPanel"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:models="clr-namespace:InfoPanel.Models"
    xmlns:pages="clr-namespace:InfoPanel.Views.Pages"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="UsbPanelsPage"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d"
    PreviewMouseWheel="Page_PreviewMouseWheel">

    <StackPanel Margin="20,20,20,20">
        <!--  start BeadaPanel Multi-Device settings  -->
        <ui:CardExpander Margin="0,0,0,0" IsExpanded="True">
            <ui:CardExpander.Icon>
                <ui:SymbolIcon Symbol="Whiteboard24" />
            </ui:CardExpander.Icon>
            <ui:CardExpander.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock
                            FontSize="13"
                            FontWeight="Medium"
                            Text="{DynamicResource Usb_BeadaPanelDisplays}" />
                        <TextBlock
                            Margin="0,5,0,0"
                            FontSize="12"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            Text="{DynamicResource Usb_BeadaPanelDisplaysDesc}" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <StackPanel Margin="0,0,10,0" Orientation="Horizontal">
                            <ui:ToggleSwitch
                                Grid.Column="1"
                                Margin="0,0,10,0"
                                IsChecked="{Binding Source={x:Static local:ConfigModel.Instance}, Path=Settings.BeadaPanelMultiDeviceMode}" />
                        </StackPanel>
                    </StackPanel>

                </Grid>
            </ui:CardExpander.Header>
            <StackPanel>
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <ui:Anchor
                            Width="150"
                            Margin="0,0,0,0"
                            VerticalAlignment="Center"
                            Appearance="Secondary"
                            Content="{DynamicResource Usb_BeadaTools}"
                            NavigateUri="https://www.nxelec.com/service/beadatools/" />
                    </StackPanel>

                    <ui:Button
                        Grid.Column="1"
                        Margin="0,0,0,0"
                        HorizontalAlignment="Right"
                        Appearance="Primary"
                        Click="ButtonDiscoverBeadaPanelDevices_Click"
                        Content="{DynamicResource Usb_DiscoverDevices}"
                        Icon="{ui:SymbolIcon Search20}"
                        Visibility="{Binding Source={x:Static local:ConfigModel.Instance}, Path=Settings.BeadaPanelMultiDeviceMode, Converter={StaticResource BooleanToVisibilityConverter}}" />
                </Grid>

                <!--  BeadaPanel Device List  -->
                <ItemsControl Margin="0,0,0,0" ItemsSource="{Binding Source={x:Static local:ConfigModel.Instance}, Path=Settings.BeadaPanelDevices}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ui:CardControl Margin="0,0,0,10">
                                <ui:CardControl.Header>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                            <TextBlock FontSize="13" FontWeight="Medium">

                                                <TextBlock.Text>
                                                    <MultiBinding StringFormat="{}{0}">
                                                        <Binding Path="RuntimeProperties.Name" />
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>
                                            <TextBlock
                                                Margin="0,0,0,0"
                                                FontSize="12"
                                                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                TextWrapping="Wrap">
                                                <TextBlock.Text>
                                                    <MultiBinding StringFormat="{}{0} - {1}">
                                                        <Binding Path="DeviceId" />
                                                        <Binding Path="DeviceLocation" />
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>
                                            <TextBlock
                                                Margin="0,0,0,0"
                                                FontSize="12"
                                                Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                                                <TextBlock.Text>
                                                    <MultiBinding StringFormat="{}Dimensions: {0}×{1} | Firmware: v{2}">
                                                        <Binding Path="RuntimeProperties.PanelInfo.ModelInfo.Width" />
                                                        <Binding Path="RuntimeProperties.PanelInfo.ModelInfo.Height" />
                                                        <Binding Path="RuntimeProperties.PanelInfo.FirmwareVersion" />
                                                        <Binding Path="RuntimeProperties.PanelInfo.Platform" />
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>
                                            <!--  Panel status when enabled  -->
                                            <StackPanel Visibility="{Binding Enabled, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <!--  Show when running  -->
                                                <StackPanel
                                                    Margin="0,5,0,0"
                                                    Orientation="Horizontal"
                                                    Visibility="{Binding RuntimeProperties.IsRunning, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                    <TextBlock
                                                        Margin="0,0,0,0"
                                                        FontSize="12"
                                                        Foreground="SeaGreen"
                                                        Text="{DynamicResource Usb_PanelIsRunning}" />
                                                    <TextBlock
                                                        Margin="10,0,0,0"
                                                        FontSize="12"
                                                        Foreground="SeaGreen"
                                                        Text="{Binding RuntimeProperties.FrameRate, StringFormat='{}{0} FPS'}" />
                                                    <TextBlock
                                                        Margin="0,0,0,0"
                                                        FontSize="12"
                                                        Foreground="SeaGreen"
                                                        Text="{Binding RuntimeProperties.FrameTime, StringFormat=' @ {0}ms'}" />
                                                </StackPanel>

                                                <!--  Show when not running  -->
                                                <TextBlock
                                                    Margin="0,5,0,0"
                                                    FontSize="12"
                                                    Foreground="Orange"
                                                    Text="{DynamicResource Usb_DisplayNotRunning}"
                                                    Visibility="{Binding RuntimeProperties.IsRunning, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>
                                </ui:CardControl.Header>
                                <StackPanel Orientation="Horizontal">
                                    <!--  Advanced settings popup  -->
                                    <StackPanel Margin="10,0,0,0" VerticalAlignment="Top">
                                        <ui:Button
                                            Width="160"
                                            Appearance="Secondary"
                                            Content="{DynamicResource Common_Advanced}"
                                            Icon="{ui:SymbolIcon Settings20}"
                                            Click="ButtonAdvancedPopup_Click" />
                                        <Popup
                                            AllowsTransparency="True"
                                            Placement="Bottom"
                                            StaysOpen="False">
                                            <Border
                                                Padding="15"
                                                Background="{DynamicResource ApplicationBackgroundBrush}"
                                                BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="8">
                                                <StackPanel Width="250">
                                                    <!--  Brightness  -->
                                                    <StackPanel>
                                                        <Slider
                                                            IsSnapToTickEnabled="True"
                                                            Maximum="100"
                                                            Minimum="0"
                                                            TickFrequency="1"
                                                            Value="{Binding Brightness}" />
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="Auto" />
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock
                                                                Margin="5,0,0,0"
                                                                FontSize="12"
                                                                Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                                                Text="{DynamicResource Common_Brightness}" />
                                                            <TextBlock
                                                                Grid.Column="1"
                                                                Margin="5,0,0,0"
                                                                FontSize="12"
                                                                Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                                                Text="{Binding Brightness}" />
                                                        </Grid>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>
                                        </Popup>
                                    </StackPanel>
                                    <StackPanel Margin="10,0,0,0">
                                        <!--  Profile selection  -->
                                        <ComboBox
                                            Width="250"
                                            Margin="0,0,0,0"
                                            DisplayMemberPath="Name"
                                            ItemsSource="{Binding Source={x:Static local:ConfigModel.Instance}, Path=Profiles}"
                                            SelectedValue="{Binding ProfileGuid}"
                                            SelectedValuePath="Guid" />
                                        <!--  Rotation  -->
                                        <ComboBox
                                            Margin="0,10,0,0"
                                            ItemsSource="{Binding Path=ViewModel.RotationValues, RelativeSource={RelativeSource AncestorType=pages:UsbPanelsPage}}"
                                            SelectedItem="{Binding Rotation}">
                                            <ComboBox.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding Converter={StaticResource EnumToDescriptionConverter}}" />
                                                </DataTemplate>
                                            </ComboBox.ItemTemplate>
                                        </ComboBox>
                                    </StackPanel>
                                    <Grid Margin="20,0,0,0">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="*" />
                                        </Grid.RowDefinitions>
                                        <!--  Enable toggle  -->
                                        <ui:ToggleSwitch
                                            Margin="0,0,0,0"
                                            VerticalAlignment="Center"
                                            IsChecked="{Binding Enabled}" />
                                        <!--  Remove button  -->
                                        <ui:Button
                                            Grid.Row="1"
                                            Width="32"
                                            Height="32"
                                            Margin="0,10,0,0"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Bottom"
                                            Appearance="Danger"
                                            Click="ButtonRemoveBeadaPanelDevice_Click"
                                            Icon="{ui:SymbolIcon Delete20}"
                                            Tag="{Binding}"
                                            ToolTip="{DynamicResource Usb_RemoveDevice}" />
                                    </Grid>
                                </StackPanel>
                            </ui:CardControl>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
            <!--  end BeadaPanel Device Management  -->
        </ui:CardExpander>

        <!--  start TuringPanel Multi-Device settings  -->
        <ui:CardExpander Margin="0,10,0,0" IsExpanded="True">
            <ui:CardExpander.Icon>
                <ui:SymbolIcon Symbol="Whiteboard24" />
            </ui:CardExpander.Icon>
            <ui:CardExpander.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock
                            FontSize="13"
                            FontWeight="Medium"
                            Text="{DynamicResource Usb_TurzxDisplays}" />
                        <TextBlock
                            Margin="0,5,0,0"
                            FontSize="12"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            Text="{DynamicResource Usb_TurzxDisplaysDesc}" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <StackPanel Margin="0,0,10,0" Orientation="Horizontal">
                            <ui:ToggleSwitch
                                Grid.Column="1"
                                Margin="0,0,10,0"
                                IsChecked="{Binding Source={x:Static local:ConfigModel.Instance}, Path=Settings.TuringPanelMultiDeviceMode}" />
                        </StackPanel>
                    </StackPanel>

                </Grid>
            </ui:CardExpander.Header>
            <StackPanel>
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <ui:Button
                        Grid.Column="1"
                        Margin="0,0,0,0"
                        HorizontalAlignment="Right"
                        Appearance="Primary"
                        Click="ButtonDiscoverTuringPanelDevices_Click"
                        Content="{DynamicResource Usb_DiscoverDevices}"
                        Icon="{ui:SymbolIcon Search20}"
                        Visibility="{Binding Source={x:Static local:ConfigModel.Instance}, Path=Settings.TuringPanelMultiDeviceMode, Converter={StaticResource BooleanToVisibilityConverter}}" />
                </Grid>

                <!--  TuringPanel Device List  -->
                <ItemsControl Margin="0,0,0,0" ItemsSource="{Binding Source={x:Static local:ConfigModel.Instance}, Path=Settings.TuringPanelDevices}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ui:CardControl Margin="0,0,0,10">
                                <ui:CardControl.Header>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                            <TextBlock FontSize="13" FontWeight="Medium">
                                                <TextBlock.Text>
                                                    <MultiBinding StringFormat="{}{0}">
                                                        <Binding Path="Name" />
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>

                                            <TextBlock
                                                Margin="0,0,0,0"
                                                FontSize="12"
                                                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                TextWrapping="Wrap">
                                                <TextBlock.Text>
                                                    <MultiBinding StringFormat="{}{0} - {1}">
                                                        <Binding Path="DeviceId" />
                                                        <Binding Path="DeviceLocation" />
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>

                                            <TextBlock
                                                Margin="0,0,0,0"
                                                FontSize="12"
                                                Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                                                <TextBlock.Text>
                                                    <MultiBinding StringFormat="{}Dimensions: {0}×{1}">
                                                        <Binding Path="ModelInfo.Width" />
                                                        <Binding Path="ModelInfo.Height" />
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>

                                            <!--  Panel status when enabled  -->
                                            <StackPanel Visibility="{Binding Enabled, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <!--  Show when running  -->
                                                <StackPanel
                                                    Margin="0,5,0,0"
                                                    Orientation="Horizontal"
                                                    Visibility="{Binding RuntimeProperties.IsRunning, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                    <TextBlock
                                                        Margin="0,0,0,0"
                                                        FontSize="12"
                                                        Foreground="SeaGreen"
                                                        Text="{DynamicResource Usb_PanelIsRunning}" />
                                                    <TextBlock
                                                        Margin="10,0,0,0"
                                                        FontSize="12"
                                                        Foreground="SeaGreen"
                                                        Text="{Binding RuntimeProperties.FrameRate, StringFormat='{}{0} FPS'}" />
                                                    <TextBlock
                                                        Margin="0,0,0,0"
                                                        FontSize="12"
                                                        Foreground="SeaGreen"
                                                        Text="{Binding RuntimeProperties.FrameTime, StringFormat=' @ {0}ms'}" />
                                                </StackPanel>

                                                <!--  Show when not running  -->
                                                <TextBlock
                                                    Margin="0,5,0,0"
                                                    FontSize="12"
                                                    Foreground="Orange"
                                                    Text="{DynamicResource Usb_DisplayNotRunning}"
                                                    Visibility="{Binding RuntimeProperties.IsRunning, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>
                                </ui:CardControl.Header>
                                <StackPanel Orientation="Horizontal">
                                    <!--  Left buttons column  -->
                                    <StackPanel Margin="10,0,0,0" VerticalAlignment="Top">
                                        <!--  Advanced settings popup  -->
                                        <ui:Button
                                            Width="160"
                                            Margin="0,0,0,10"
                                            Appearance="Secondary"
                                            Content="{DynamicResource Common_Advanced}"
                                            Icon="{ui:SymbolIcon Settings20}"
                                            Click="ButtonAdvancedPopup_Click" />
                                        <!--  Manage Storage button (disabled — no longer supported)  -->
                                        <!--<ui:Button
                                            Width="160"
                                            Click="ButtonManageTuringDevice_Click"
                                            Content="{DynamicResource Usb_ManageStorage}"
                                            Icon="{ui:SymbolIcon Settings24}"
                                            IsEnabled="{Binding Enabled, Converter={StaticResource InverseBooleanConverter}}"
                                            Tag="{Binding}"
                                            ToolTip="{DynamicResource Usb_ManageStorageDesc}"
                                            Visibility="{Binding ModelInfo.IsUsbDevice, Converter={StaticResource BooleanToVisibilityConverter}}" />-->
                                        <Popup
                                            AllowsTransparency="True"
                                            Placement="Bottom"
                                            StaysOpen="False">
                                            <Border
                                                Padding="15"
                                                Background="{DynamicResource ApplicationBackgroundBrush}"
                                                BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="8">
                                                <StackPanel Width="250">
                                                    <!--  Brightness  -->
                                                    <StackPanel>
                                                        <Slider
                                                            IsSnapToTickEnabled="True"
                                                            Maximum="100"
                                                            Minimum="0"
                                                            TickFrequency="1"
                                                            Value="{Binding Brightness}" />
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="Auto" />
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock
                                                                Margin="5,0,0,0"
                                                                FontSize="12"
                                                                Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                                                Text="{DynamicResource Common_Brightness}" />
                                                            <TextBlock
                                                                Grid.Column="1"
                                                                Margin="5,0,0,0"
                                                                FontSize="12"
                                                                Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                                                Text="{Binding Brightness}" />
                                                        </Grid>
                                                    </StackPanel>
                                                    <!--  Target FPS (USB devices only)  -->
                                                    <StackPanel Margin="0,10,0,0"
                                                                Visibility="{Binding ModelInfo.IsUsbDevice, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                        <Slider
                                                            IsSnapToTickEnabled="True"
                                                            Maximum="30"
                                                            Minimum="1"
                                                            TickFrequency="1"
                                                            Value="{Binding TargetFrameRate}" />
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="Auto" />
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock
                                                                Margin="5,0,0,0"
                                                                FontSize="12"
                                                                Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                                                Text="{DynamicResource Usb_TargetFPS}" />
                                                            <TextBlock
                                                                Grid.Column="1"
                                                                Margin="5,0,0,0"
                                                                FontSize="12"
                                                                Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                                                Text="{Binding TargetFrameRate}" />
                                                        </Grid>
                                                    </StackPanel>
                                                    <!--  JPEG Quality (USB devices only)  -->
                                                    <StackPanel Margin="0,10,0,0"
                                                                Visibility="{Binding ModelInfo.IsUsbDevice, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                        <Slider
                                                            IsSnapToTickEnabled="True"
                                                            Maximum="100"
                                                            Minimum="50"
                                                            TickFrequency="5"
                                                            Value="{Binding JpegQuality}" />
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="Auto" />
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock
                                                                Margin="5,0,0,0"
                                                                FontSize="12"
                                                                Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                                                Text="{DynamicResource Usb_JPEGQuality}" />
                                                            <TextBlock
                                                                Grid.Column="1"
                                                                Margin="5,0,0,0"
                                                                FontSize="12"
                                                                Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                                                Text="{Binding JpegQuality}" />
                                                        </Grid>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>
                                        </Popup>
                                    </StackPanel>
                                    <StackPanel Margin="10,0,0,0">
                                        <!--  Profile selection  -->
                                        <ComboBox
                                            Width="250"
                                            Margin="0,0,0,0"
                                            DisplayMemberPath="Name"
                                            ItemsSource="{Binding Source={x:Static local:ConfigModel.Instance}, Path=Profiles}"
                                            SelectedValue="{Binding ProfileGuid}"
                                            SelectedValuePath="Guid" />
                                        <!--  Rotation  -->
                                        <ComboBox
                                            Margin="0,10,0,0"
                                            ItemsSource="{Binding Path=ViewModel.RotationValues, RelativeSource={RelativeSource AncestorType=pages:UsbPanelsPage}}"
                                            SelectedItem="{Binding Rotation}">
                                            <ComboBox.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding Converter={StaticResource EnumToDescriptionConverter}}" />
                                                </DataTemplate>
                                            </ComboBox.ItemTemplate>
                                        </ComboBox>
                                    </StackPanel>
                                    <Grid Margin="20,0,0,0">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="*" />
                                        </Grid.RowDefinitions>
                                        <!--  Enable toggle  -->
                                        <ui:ToggleSwitch
                                            Margin="0,0,0,0"
                                            VerticalAlignment="Center"
                                            IsChecked="{Binding Enabled}" />
                                        <!--  Remove button  -->
                                        <ui:Button
                                            Grid.Row="1"
                                            Width="32"
                                            Height="32"
                                            Margin="0,10,0,0"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Bottom"
                                            Appearance="Danger"
                                            Click="ButtonRemoveTuringPanelDevice_Click"
                                            Icon="{ui:SymbolIcon Delete20}"
                                            Tag="{Binding}"
                                            ToolTip="{DynamicResource Usb_RemoveDevice}" />
                                    </Grid>
                                </StackPanel>
                            </ui:CardControl>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
            <!--  end TuringPanel Device Management  -->
        </ui:CardExpander>
        <!--  end TuringPanel Multi-Device settings  -->

        <!--  start ThermalrightPanel Multi-Device settings  -->
        <ui:CardExpander
            Margin="0,10,0,0"
            IsExpanded="True">
            <ui:CardExpander.Icon>
                <ui:SymbolIcon Symbol="Whiteboard24" />
            </ui:CardExpander.Icon>
            <ui:CardExpander.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock
                            FontSize="13"
                            FontWeight="Medium"
                            Text="{DynamicResource Usb_ThermalrightDisplays}" />
                        <TextBlock
                            Margin="0,5,0,0"
                            FontSize="12"
                            Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                            Text="{DynamicResource Usb_ThermalrightDisplaysDesc}" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <StackPanel Margin="0,0,10,0" Orientation="Horizontal">
                            <ui:ToggleSwitch
                                Grid.Column="1"
                                Margin="0,0,10,0"
                                IsChecked="{Binding Source={x:Static local:ConfigModel.Instance}, Path=Settings.ThermalrightPanelMultiDeviceMode}" />
                        </StackPanel>
                    </StackPanel>

                </Grid>
            </ui:CardExpander.Header>
            <StackPanel>
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <ui:Button
                        Grid.Column="1"
                        Margin="0,0,0,0"
                        HorizontalAlignment="Right"
                        Appearance="Primary"
                        Click="ButtonDiscoverThermalrightPanelDevices_Click"
                        Content="{DynamicResource Usb_DiscoverDevices}"
                        Icon="{ui:SymbolIcon Search20}"
                        Visibility="{Binding Source={x:Static local:ConfigModel.Instance}, Path=Settings.ThermalrightPanelMultiDeviceMode, Converter={StaticResource BooleanToVisibilityConverter}}" />
                </Grid>

                <!--  ThermalrightPanel Device List  -->
                <ItemsControl Margin="0,0,0,0" ItemsSource="{Binding Source={x:Static local:ConfigModel.Instance}, Path=Settings.ThermalrightPanelDevices}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ui:CardControl Margin="0,0,0,10">
                                <ui:CardControl.Header>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                            <TextBlock FontSize="13" FontWeight="Medium">
                                                <TextBlock.Text>
                                                    <MultiBinding StringFormat="{}{0}">
                                                        <Binding Path="RuntimeProperties.Name" />
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                                <TextBlock.ToolTip>
                                                    <TextBlock Text="{Binding RuntimeProperties.DeviceInfo}" />
                                                </TextBlock.ToolTip>
                                            </TextBlock>

                                            <TextBlock
                                                Margin="0,0,0,0"
                                                FontSize="12"
                                                Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                                TextWrapping="Wrap">
                                                <TextBlock.Text>
                                                    <MultiBinding StringFormat="{}{0} - {1}">
                                                        <Binding Path="DeviceId" />
                                                        <Binding Path="DeviceLocation" />
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>

                                            <TextBlock
                                                Margin="0,0,0,0"
                                                FontSize="12"
                                                Foreground="{DynamicResource TextFillColorTertiaryBrush}">
                                                <TextBlock.Text>
                                                    <MultiBinding StringFormat="{}Dimensions: {0}x{1}">
                                                        <Binding Path="DisplayWidth" />
                                                        <Binding Path="DisplayHeight" />
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>

                                            <!--  Panel status when enabled  -->
                                            <StackPanel Visibility="{Binding Enabled, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <!--  Show when running  -->
                                                <StackPanel
                                                    Margin="0,5,0,0"
                                                    Orientation="Horizontal"
                                                    Visibility="{Binding RuntimeProperties.IsRunning, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                    <TextBlock
                                                        Margin="0,0,0,0"
                                                        FontSize="12"
                                                        Foreground="LawnGreen"
                                                        Text="{DynamicResource Usb_PanelIsRunning}" />
                                                    <TextBlock
                                                        Margin="10,0,0,0"
                                                        FontSize="12"
                                                        Foreground="LawnGreen"
                                                        Text="{Binding RuntimeProperties.FrameRate, StringFormat='{}{0} FPS'}" />
                                                    <TextBlock
                                                        Margin="0,0,0,0"
                                                        FontSize="12"
                                                        Foreground="LawnGreen"
                                                        Text="{Binding RuntimeProperties.FrameTime, StringFormat=' @ {0}ms'}" />
                                                </StackPanel>

                                                <!--  Show when not running  -->
                                                <StackPanel
                                                    Margin="0,5,0,0"
                                                    Visibility="{Binding RuntimeProperties.IsRunning, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                                                    <TextBlock
                                                        FontSize="12"
                                                        Foreground="Orange"
                                                        Text="{DynamicResource Usb_DisplayNotRunning}" />
                                                    <TextBlock
                                                        FontSize="12"
                                                        Foreground="Orange"
                                                        Text="{Binding RuntimeProperties.ErrorMessage}"
                                                        Visibility="{Binding RuntimeProperties.ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" />
                                                </StackPanel>
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>
                                </ui:CardControl.Header>
                                <StackPanel Orientation="Horizontal">
                                    <!--  Advanced settings popup  -->
                                    <StackPanel Margin="10,0,0,0" VerticalAlignment="Top">
                                        <ui:Button
                                            Width="160"
                                            Appearance="Secondary"
                                            Content="{DynamicResource Common_Advanced}"
                                            Icon="{ui:SymbolIcon Settings20}"
                                            Click="ButtonAdvancedPopup_Click" />
                                        <Popup
                                            AllowsTransparency="True"
                                            Placement="Bottom"
                                            StaysOpen="False">
                                            <Border
                                                Padding="15"
                                                Background="{DynamicResource ApplicationBackgroundBrush}"
                                                BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="8">
                                                <StackPanel Width="250">
                                                    <!--  Brightness  -->
                                                    <StackPanel>
                                                        <Slider
                                                            IsSnapToTickEnabled="True"
                                                            Maximum="100"
                                                            Minimum="0"
                                                            TickFrequency="1"
                                                            Value="{Binding Brightness}" />
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="Auto" />
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock
                                                                Margin="5,0,0,0"
                                                                FontSize="12"
                                                                Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                                                Text="{DynamicResource Common_Brightness}" />
                                                            <TextBlock
                                                                Grid.Column="1"
                                                                Margin="5,0,0,0"
                                                                FontSize="12"
                                                                Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                                                Text="{Binding Brightness}" />
                                                        </Grid>
                                                    </StackPanel>
                                                    <!--  Target FPS  -->
                                                    <StackPanel Margin="0,10,0,0">
                                                            <Slider
                                                                IsSnapToTickEnabled="True"
                                                                Maximum="30"
                                                                Minimum="1"
                                                                TickFrequency="1"
                                                                Value="{Binding TargetFrameRate}" />
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*" />
                                                                    <ColumnDefinition Width="Auto" />
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock
                                                                    Margin="5,0,0,0"
                                                                    FontSize="12"
                                                                    Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                                                    Text="{DynamicResource Usb_TargetFPS}" />
                                                                <TextBlock
                                                                    Grid.Column="1"
                                                                    Margin="5,0,0,0"
                                                                    FontSize="12"
                                                                    Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                                                    Text="{Binding TargetFrameRate}" />
                                                            </Grid>
                                                        </StackPanel>
                                                        <!--  JPEG Quality (hidden for RGB565-only panels)  -->
                                                        <StackPanel Margin="0,10,0,0"
                                                                    Visibility="{Binding IsJpegQualityConfigurable, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                            <Slider
                                                                IsSnapToTickEnabled="True"
                                                                Maximum="100"
                                                                Minimum="50"
                                                                TickFrequency="5"
                                                                Value="{Binding JpegQuality}" />
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*" />
                                                                    <ColumnDefinition Width="Auto" />
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock
                                                                    Margin="5,0,0,0"
                                                                    FontSize="12"
                                                                    Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                                                    Text="{DynamicResource Usb_JPEGQuality}" />
                                                                <TextBlock
                                                                    Grid.Column="1"
                                                                    Margin="5,0,0,0"
                                                                    FontSize="12"
                                                                    Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                                                    Text="{Binding JpegQuality}" />
                                                            </Grid>
                                                        </StackPanel>
                                                        <!--  Flicker Fix (TrofeoBulk 5408: crop JPEG to 462 rows)  -->
                                                        <Grid Margin="0,10,0,0"
                                                              Visibility="{Binding HasFlickerFix, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="Auto" />
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock
                                                                VerticalAlignment="Center"
                                                                FontSize="12"
                                                                Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                                                Text="{DynamicResource Usb_FlickerFix}" />
                                                            <ui:ToggleSwitch
                                                                Grid.Column="1"
                                                                IsChecked="{Binding FlickerFix}" />
                                                        </Grid>
                                                        <!--  Display Mask (Wonder/Rainbow Vision 360 punch-hole cover)  -->
                                                        <Grid Margin="0,10,0,0"
                                                              Visibility="{Binding HasDisplayMask, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="Auto" />
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock
                                                                VerticalAlignment="Center"
                                                                FontSize="12"
                                                                Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                                                Text="{DynamicResource Usb_Mask}" />
                                                            <ComboBox
                                                                Grid.Column="1"
                                                                Width="150"
                                                                ItemsSource="{Binding Path=ViewModel.DisplayMaskValues, RelativeSource={RelativeSource AncestorType=pages:UsbPanelsPage}}"
                                                                SelectedItem="{Binding DisplayMask}">
                                                                <ComboBox.ItemTemplate>
                                                                    <DataTemplate>
                                                                        <TextBlock Text="{Binding Converter={StaticResource EnumToDescriptionConverter}}" />
                                                                    </DataTemplate>
                                                                </ComboBox.ItemTemplate>
                                                            </ComboBox>
                                                        </Grid>
                                                    </StackPanel>
                                                </Border>
                                            </Popup>
                                        </StackPanel>
                                    <StackPanel Margin="10,0,0,0">
                                        <!--  Profile selection  -->
                                        <ComboBox
                                            Width="250"
                                            Margin="0,0,0,0"
                                            DisplayMemberPath="Name"
                                            ItemsSource="{Binding Source={x:Static local:ConfigModel.Instance}, Path=Profiles}"
                                            SelectedValue="{Binding ProfileGuid}"
                                            SelectedValuePath="Guid" />
                                        <!--  Rotation  -->
                                        <ComboBox
                                            Margin="0,10,0,0"
                                            ItemsSource="{Binding Path=ViewModel.RotationValues, RelativeSource={RelativeSource AncestorType=pages:UsbPanelsPage}}"
                                            SelectedItem="{Binding Rotation}">
                                            <ComboBox.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding Converter={StaticResource EnumToDescriptionConverter}}" />
                                                </DataTemplate>
                                            </ComboBox.ItemTemplate>
                                        </ComboBox>
                                    </StackPanel>
                                    <Grid Margin="20,0,0,0">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="*" />
                                        </Grid.RowDefinitions>
                                        <!--  Enable toggle  -->
                                        <ui:ToggleSwitch
                                            Margin="0,0,0,0"
                                            VerticalAlignment="Center"
                                            IsChecked="{Binding Enabled}" />
                                        <!--  Remove button  -->
                                        <ui:Button
                                            Grid.Row="1"
                                            Width="32"
                                            Height="32"
                                            Margin="0,10,0,0"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Bottom"
                                            Appearance="Danger"
                                            BorderThickness="0"
                                            Click="ButtonRemoveThermalrightPanelDevice_Click"
                                            Icon="{ui:SymbolIcon Delete20}"
                                            Tag="{Binding}"
                                            ToolTip="{DynamicResource Usb_RemoveDevice}" />
                                    </Grid>
                                </StackPanel>
                            </ui:CardControl>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
            <!--  end ThermalrightPanel Device Management  -->
        </ui:CardExpander>
        <!--  end ThermalrightPanel Multi-Device settings  -->

        <!--  ThermaltakePanel Multi-Device settings  -->
        <ui:CardExpander
            Margin="0,10,0,0"
            IsExpanded="True">
            <ui:CardExpander.Icon>
                <ui:SymbolIcon Symbol="Whiteboard24" />
            </ui:CardExpander.Icon>
            <ui:CardExpander.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock
                            FontSize="13"
                            FontWeight="Medium"
                            Text="{DynamicResource Usb_ThermaltakeAsrock}" />
                        <TextBlock
                            Margin="0,5,0,0"
                            FontSize="12"
                            Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                            Text="{DynamicResource Usb_ThermaltakeAsrockDesc}" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <StackPanel Margin="0,0,10,0" Orientation="Horizontal">
                            <ui:ToggleSwitch
                                Grid.Column="1"
                                Margin="0,0,10,0"
                                IsChecked="{Binding Source={x:Static local:ConfigModel.Instance}, Path=Settings.ThermaltakePanelMultiDeviceMode}" />
                        </StackPanel>
                    </StackPanel>

                </Grid>
            </ui:CardExpander.Header>
            <StackPanel>
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <ui:Button
                        Grid.Column="1"
                        Margin="0,0,0,0"
                        HorizontalAlignment="Right"
                        Appearance="Primary"
                        Click="ButtonDiscoverThermaltakePanelDevices_Click"
                        Content="{DynamicResource Usb_DiscoverDevices}"
                        Icon="{ui:SymbolIcon Search20}"
                        Visibility="{Binding Source={x:Static local:ConfigModel.Instance}, Path=Settings.ThermaltakePanelMultiDeviceMode, Converter={StaticResource BooleanToVisibilityConverter}}" />
                </Grid>

                <!--  ThermaltakePanel Device List  -->
                <ItemsControl Margin="0,0,0,0" ItemsSource="{Binding Source={x:Static local:ConfigModel.Instance}, Path=Settings.ThermaltakePanelDevices}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ui:CardControl Margin="0,0,0,10">
                                <ui:CardControl.Header>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                            <TextBlock FontSize="13" FontWeight="Medium">
                                                <TextBlock.Text>
                                                    <MultiBinding StringFormat="{}{0}">
                                                        <Binding Path="RuntimeProperties.Name" />
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>

                                            <TextBlock
                                                Margin="0,0,0,0"
                                                FontSize="12"
                                                Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                                TextWrapping="Wrap">
                                                <TextBlock.Text>
                                                    <MultiBinding StringFormat="{}{0} - {1}">
                                                        <Binding Path="DeviceId" />
                                                        <Binding Path="DeviceLocation" />
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>

                                            <TextBlock
                                                Margin="0,0,0,0"
                                                FontSize="12"
                                                Foreground="{DynamicResource TextFillColorTertiaryBrush}">
                                                <TextBlock.Text>
                                                    <MultiBinding StringFormat="{}Dimensions: {0}x{1}">
                                                        <Binding Path="DisplayWidth" />
                                                        <Binding Path="DisplayHeight" />
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>

                                            <!--  Panel status when enabled  -->
                                            <StackPanel Visibility="{Binding Enabled, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <!--  Show when running  -->
                                                <StackPanel
                                                    Margin="0,5,0,0"
                                                    Orientation="Horizontal"
                                                    Visibility="{Binding RuntimeProperties.IsRunning, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                    <TextBlock
                                                        Margin="0,0,0,0"
                                                        FontSize="12"
                                                        Foreground="LawnGreen"
                                                        Text="{DynamicResource Usb_PanelIsRunning}" />
                                                    <TextBlock
                                                        Margin="10,0,0,0"
                                                        FontSize="12"
                                                        Foreground="LawnGreen"
                                                        Text="{Binding RuntimeProperties.FrameRate, StringFormat='{}{0} FPS'}" />
                                                    <TextBlock
                                                        Margin="0,0,0,0"
                                                        FontSize="12"
                                                        Foreground="LawnGreen"
                                                        Text="{Binding RuntimeProperties.FrameTime, StringFormat=' @ {0}ms'}" />
                                                </StackPanel>

                                                <!--  Show when not running  -->
                                                <StackPanel
                                                    Margin="0,5,0,0"
                                                    Visibility="{Binding RuntimeProperties.IsRunning, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                                                    <TextBlock
                                                        FontSize="12"
                                                        Foreground="Orange"
                                                        Text="{DynamicResource Usb_DisplayNotRunning}" />
                                                    <TextBlock
                                                        FontSize="12"
                                                        Foreground="Orange"
                                                        Text="{Binding RuntimeProperties.ErrorMessage}"
                                                        Visibility="{Binding RuntimeProperties.ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" />
                                                </StackPanel>
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>
                                </ui:CardControl.Header>
                                <StackPanel Orientation="Horizontal">
                                    <!--  Advanced settings popup  -->
                                    <StackPanel Margin="10,0,0,0" VerticalAlignment="Top">
                                        <ui:Button
                                            Width="160"
                                            Appearance="Secondary"
                                            Content="{DynamicResource Common_Advanced}"
                                            Icon="{ui:SymbolIcon Settings20}"
                                            Click="ButtonAdvancedPopup_Click" />
                                        <Popup
                                            AllowsTransparency="True"
                                            Placement="Bottom"
                                            StaysOpen="False">
                                            <Border
                                                Padding="15"
                                                Background="{DynamicResource ApplicationBackgroundBrush}"
                                                BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="8">
                                                <StackPanel Width="250">
                                                    <!--  Brightness (hardware)  -->
                                                    <StackPanel>
                                                        <Slider
                                                            IsSnapToTickEnabled="True"
                                                            Maximum="100"
                                                            Minimum="0"
                                                            TickFrequency="1"
                                                            Value="{Binding Brightness}" />
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="Auto" />
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock
                                                                Margin="5,0,0,0"
                                                                FontSize="12"
                                                                Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                                                Text="{DynamicResource Common_Brightness}" />
                                                            <TextBlock
                                                                Grid.Column="1"
                                                                Margin="5,0,0,0"
                                                                FontSize="12"
                                                                Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                                                Text="{Binding Brightness}" />
                                                        </Grid>
                                                    </StackPanel>
                                                    <!--  Target FPS  -->
                                                    <StackPanel Margin="0,10,0,0">
                                                            <Slider
                                                                IsSnapToTickEnabled="True"
                                                                Maximum="30"
                                                                Minimum="1"
                                                                TickFrequency="1"
                                                                Value="{Binding TargetFrameRate}" />
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*" />
                                                                    <ColumnDefinition Width="Auto" />
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock
                                                                    Margin="5,0,0,0"
                                                                    FontSize="12"
                                                                    Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                                                    Text="{DynamicResource Usb_TargetFPS}" />
                                                                <TextBlock
                                                                    Grid.Column="1"
                                                                    Margin="5,0,0,0"
                                                                    FontSize="12"
                                                                    Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                                                    Text="{Binding TargetFrameRate}" />
                                                            </Grid>
                                                        </StackPanel>
                                                        <!--  JPEG Quality  -->
                                                        <StackPanel Margin="0,10,0,0">
                                                            <Slider
                                                                IsSnapToTickEnabled="True"
                                                                Maximum="100"
                                                                Minimum="50"
                                                                TickFrequency="5"
                                                                Value="{Binding JpegQuality}" />
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*" />
                                                                    <ColumnDefinition Width="Auto" />
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock
                                                                    Margin="5,0,0,0"
                                                                    FontSize="12"
                                                                    Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                                                    Text="{DynamicResource Usb_JPEGQuality}" />
                                                                <TextBlock
                                                                    Grid.Column="1"
                                                                    Margin="5,0,0,0"
                                                                    FontSize="12"
                                                                    Foreground="{DynamicResource TextFillColorDisabledBrush}"
                                                                    Text="{Binding JpegQuality}" />
                                                            </Grid>
                                                        </StackPanel>
                                                </StackPanel>
                                            </Border>
                                        </Popup>
                                    </StackPanel>
                                    <StackPanel Margin="10,0,0,0">
                                        <!--  Profile selection  -->
                                        <ComboBox
                                            Width="250"
                                            Margin="0,0,0,0"
                                            DisplayMemberPath="Name"
                                            ItemsSource="{Binding Source={x:Static local:ConfigModel.Instance}, Path=Profiles}"
                                            SelectedValue="{Binding ProfileGuid}"
                                            SelectedValuePath="Guid" />
                                        <!--  Rotation  -->
                                        <ComboBox
                                            Margin="0,10,0,0"
                                            ItemsSource="{Binding Path=ViewModel.RotationValues, RelativeSource={RelativeSource AncestorType=pages:UsbPanelsPage}}"
                                            SelectedItem="{Binding Rotation}">
                                            <ComboBox.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding Converter={StaticResource EnumToDescriptionConverter}}" />
                                                </DataTemplate>
                                            </ComboBox.ItemTemplate>
                                        </ComboBox>
                                    </StackPanel>
                                    <Grid Margin="20,0,0,0">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="*" />
                                        </Grid.RowDefinitions>
                                        <!--  Enable toggle  -->
                                        <ui:ToggleSwitch
                                            Margin="0,0,0,0"
                                            VerticalAlignment="Center"
                                            IsChecked="{Binding Enabled}" />
                                        <!--  Remove button  -->
                                        <ui:Button
                                            Grid.Row="1"
                                            Width="32"
                                            Height="32"
                                            Margin="0,10,0,0"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Bottom"
                                            Appearance="Danger"
                                            BorderThickness="0"
                                            Click="ButtonRemoveThermaltakePanelDevice_Click"
                                            Icon="{ui:SymbolIcon Delete20}"
                                            Tag="{Binding}"
                                            ToolTip="{DynamicResource Usb_RemoveDevice}" />
                                    </Grid>
                                </StackPanel>
                            </ui:CardControl>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ui:CardExpander>
        <!--  end ThermaltakePanel Multi-Device settings  -->

        <!--  Global Hotkeys  -->
        <ui:CardExpander Margin="0,20,0,0" IsExpanded="False">
            <ui:CardExpander.Icon>
                <ui:SymbolIcon Symbol="Keyboard24" />
            </ui:CardExpander.Icon>
            <ui:CardExpander.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock
                            FontSize="13"
                            FontWeight="Medium"
                            Text="{DynamicResource Usb_GlobalHotkeys}" />
                        <TextBlock
                            Margin="0,5,0,0"
                            FontSize="12"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            Text="{DynamicResource Usb_GlobalHotkeysDesc}" />
                    </StackPanel>
                </Grid>
            </ui:CardExpander.Header>
            <StackPanel>
                <ui:InfoBar
                    Margin="0,0,0,5"
                    IsClosable="False"
                    IsOpen="True"
                    Message="Global hotkeys are system-wide and will override the same shortcut in other applications. Use multi-modifier combos to avoid conflicts (e.g. Ctrl+Alt+1)."
                    Severity="Warning" />
                <!--  Existing hotkey bindings  -->
                <ItemsControl ItemsSource="{Binding Source={x:Static local:ConfigModel.Instance}, Path=Settings.HotkeyBindings}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:HotkeyBinding}">
                            <ui:CardControl Margin="0,5,0,0">
                                <ui:CardControl.Header>
                                    <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                                        <TextBlock
                                            MinWidth="120"
                                            Margin="0,0,15,0"
                                            VerticalAlignment="Center"
                                            FontSize="13"
                                            FontWeight="Medium"
                                            Text="{Binding HotkeyDisplayText, Mode=OneWay}" />
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock FontSize="12">
                                                <TextBlock.Text>
                                                    <MultiBinding StringFormat="{}Switch {0} to profile {1}">
                                                        <Binding Mode="OneWay" Path="DeviceDisplayName" />
                                                        <Binding Mode="OneWay" Path="ProfileDisplayName" />
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>
                                            <TextBlock
                                                FontSize="12"
                                                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                Text="{Binding DeviceId}" />
                                        </StackPanel>
                                    </StackPanel>
                                </ui:CardControl.Header>
                                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                                    <ui:Button
                                        Click="ButtonEditHotkey_Click"
                                        Icon="{ui:SymbolIcon Edit20}"
                                        Tag="{Binding}"
                                        ToolTip="{DynamicResource Usb_EditHotkey}" />
                                    <ui:Button
                                        Margin="5,0,0,0"
                                        Appearance="Danger"
                                        Click="ButtonRemoveHotkey_Click"
                                        Icon="{ui:SymbolIcon Delete20}"
                                        Tag="{Binding}"
                                        ToolTip="{DynamicResource Usb_RemoveHotkey}" />
                                </StackPanel>
                            </ui:CardControl>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!--  Add new hotkey  -->
                <ui:CardControl Margin="0,10,0,0" Padding="15">
                    <StackPanel>
                        <WrapPanel Orientation="Horizontal">
                            <StackPanel Margin="0,0,10,5">
                                <TextBlock
                                    Margin="0,0,0,5"
                                    FontSize="12"
                                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                    Text="{DynamicResource Usb_Hotkey}" />
                                <TextBox
                                    x:Name="HotkeyCapture"
                                    Width="200"
                                    GotFocus="HotkeyCapture_GotFocus"
                                    IsReadOnly="True"
                                    PreviewKeyDown="HotkeyCapture_PreviewKeyDown"
                                    Text="{DynamicResource Usb_HotkeyPrompt}" />
                            </StackPanel>
                            <StackPanel Margin="0,0,10,5">
                                <TextBlock
                                    Margin="0,0,0,5"
                                    FontSize="12"
                                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                    Text="{DynamicResource Common_Panel}" />
                                <ComboBox
                                    x:Name="HotkeyDeviceCombo"
                                    Width="200"
                                    DisplayMemberPath="Item2"
                                    SelectedValuePath="Item1" />
                            </StackPanel>
                            <StackPanel Margin="0,0,0,5">
                                <TextBlock
                                    Margin="0,0,0,5"
                                    FontSize="12"
                                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                    Text="{DynamicResource Usb_Profile}" />
                                <ComboBox
                                    x:Name="HotkeyProfileCombo"
                                    Width="200"
                                    DisplayMemberPath="Name"
                                    ItemsSource="{Binding Source={x:Static local:ConfigModel.Instance}, Path=Profiles}"
                                    SelectedValuePath="Guid" />
                            </StackPanel>
                        </WrapPanel>
                        <StackPanel
                            Margin="0,5,0,0"
                            HorizontalAlignment="Right"
                            Orientation="Horizontal">
                            <ui:Button
                                x:Name="HotkeyCancelButton"
                                Margin="0,0,5,0"
                                BorderThickness="0"
                                Click="ButtonCancelHotkey_Click"
                                Content="{DynamicResource Common_Cancel}"
                                Visibility="Collapsed" />
                            <ui:Button
                                x:Name="HotkeyAddButton"
                                Appearance="Primary"
                                BorderThickness="0"
                                Click="ButtonAddHotkey_Click"
                                Content="{DynamicResource Common_Add}"
                                Icon="{ui:SymbolIcon Add20}" />
                        </StackPanel>
                    </StackPanel>
                </ui:CardControl>
            </StackPanel>
        </ui:CardExpander>
        <!--  end Global Hotkeys  -->
    </StackPanel>
</Page>