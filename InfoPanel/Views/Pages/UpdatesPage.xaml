<Page
    x:Class="InfoPanel.Views.Pages.UpdatesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:components="clr-namespace:InfoPanel.Views.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:InfoPanel"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewmodels="clr-namespace:InfoPanel.ViewModels"
    Title="AboutPage"
    d:DataContext="{d:DesignInstance Type=viewmodels:UpdatesViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <StackPanel Margin="20,20,20,20">
                <StackPanel Grid.Row="0" Margin="0,0,0,0">
                    <ui:CardControl>
                        <ui:CardControl.Icon>
                            <ui:SymbolIcon Symbol="PhoneUpdate20" />
                        </ui:CardControl.Icon>
                        <ui:CardControl.Header>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="auto" />
                                    <ColumnDefinition Width="auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock
                                        FontSize="13"
                                        FontWeight="Medium">
                                        <Run Text="InfoPanel v" /><Run Text="{Binding Version, Mode=OneWay}" />
                                    </TextBlock>

                                    <TextBlock Margin="0,5,0,0" FontSize="12">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
                                                <Setter Property="Text" Value="{DynamicResource Updates_NoUpdatesAvailable}" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding UpdateAvailable}" Value="True">
                                                        <Setter Property="Foreground" Value="{DynamicResource SystemAccentColorPrimaryBrush}" />
                                                        <Setter Property="Text" Value="{DynamicResource Updates_UpdateAvailableFormat}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>

                                </StackPanel>
                                <ui:Button
                                    Grid.Column="1"
                                    Width="150"
                                    Margin="0,0,0,0"
                                    Appearance="Primary"
                                    Click="ButtonUpdate_Click"
                                    Content="{DynamicResource Updates_Update}"
                                    IsEnabled="{Binding DownloadInProgress, Converter={StaticResource InverseBooleanConverter}}"
                                    Visibility="{Binding UpdateAvailable, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                <ui:Button
                                    Grid.Column="2"
                                    Width="150"
                                    Margin="10,0,0,0"
                                    Click="ButtonCheckUpdates_Click"
                                    Content="{DynamicResource Updates_CheckForUpdates}"
                                    IsEnabled="{Binding UpdateCheckInProgress, Converter={StaticResource InverseBooleanConverter}}" />
                            </Grid>
                        </ui:CardControl.Header>
                    </ui:CardControl>

                    <StackPanel
                        Margin="0,10,0,0"
                        Visibility="{Binding DownloadInProgress, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <ProgressBar
                            Name="ProgressBarDownload"
                            Height="7"
                            Maximum="100"
                            Minimum="0"
                            Value="{Binding DownloadProgress}" />
                        <TextBlock
                            Margin="0,4,0,0"
                            HorizontalAlignment="Center"
                            FontSize="11"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            Text="{Binding DownloadStatus}" />
                    </StackPanel>

                    <Label
                        Margin="0,30,0,0"
                        Content="{DynamicResource Updates_WhatsNew}"
                        FontSize="14"
                        FontWeight="Medium"
                        Foreground="{DynamicResource TextFillColorPrimaryBrush}" />

                    <ItemsControl ItemsSource="{Binding UpdateVersions}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <!-- Layout with CardExpander (when Summary exists) -->
                                    <ui:CardExpander
                                        Margin="0,10,0,0"
                                        HorizontalContentAlignment="Left"
                                        IsExpanded="{Binding Expanded}">
                                        <ui:CardExpander.Style>
                                            <Style TargetType="ui:CardExpander" BasedOn="{StaticResource {x:Type ui:CardExpander}}">
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding HasSummary}" Value="False">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ui:CardExpander.Style>
                                        <ui:CardExpander.Header>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock
                                                    FontSize="13"
                                                    FontWeight="Medium"
                                                    Text="{Binding Version}" />
                                                <TextBlock
                                                    Margin="0,5,0,0"
                                                    FontSize="12"
                                                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                    TextWrapping="Wrap"
                                                    Loaded="SummaryTextBlock_Loaded" />
                                            </StackPanel>
                                        </ui:CardExpander.Header>
                                        <TextBlock
                                            FontSize="12"
                                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                            TextWrapping="Wrap"
                                            Loaded="DetailsTextBlock_Loaded" />
                                    </ui:CardExpander>

                                    <!-- Layout without CardExpander (when no Summary) -->
                                    <ui:Card Margin="0,10,0,0" Padding="16">
                                        <ui:Card.Style>
                                            <Style TargetType="ui:Card" BasedOn="{StaticResource {x:Type ui:Card}}">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding HasSummary}" Value="False">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ui:Card.Style>
                                        <StackPanel>
                                            <TextBlock
                                                FontSize="13"
                                                FontWeight="Medium"
                                                Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                                Text="{Binding Version}" />
                                            <TextBlock
                                                Margin="0,5,0,0"
                                                FontSize="12"
                                                Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                                TextWrapping="Wrap"
                                                Loaded="ChangelogTextBlock_Loaded" />
                                        </StackPanel>
                                    </ui:Card>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </StackPanel>
</Page>
