<ui:FluentWindow
    x:Class="InfoPanel.Views.Windows.TuringDeviceWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="{DynamicResource Turing_Title}"
    Width="900"
    Height="600"
    ExtendsContentIntoTitleBar="True"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            Title="{Binding DeviceName, FallbackValue='Turing Device Manager'}"
            Grid.Row="0"
            ShowMaximize="False"
            ShowMinimize="False" />

        <TabControl Grid.Row="1" Margin="20,20,20,20">
            <!--  Video Management Tab  -->
            <TabItem Header="{DynamicResource Turing_VideoManagement}">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!--  Storage Info  -->
                    <ui:Card Grid.Row="0" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock
                                Margin="0,0,0,10"
                                FontSize="13"
                                FontWeight="Medium"
                                Text="{DynamicResource Turing_StorageInfo}" />
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock
                                    Grid.Row="0"
                                    Grid.Column="0"
                                    Margin="0,0,10,5"
                                    Text="{DynamicResource Turing_TotalSpace}" />
                                <TextBlock
                                    Grid.Row="0"
                                    Grid.Column="1"
                                    Margin="0,0,0,5"
                                    Text="{Binding TotalStorageDisplay}" />

                                <TextBlock
                                    Grid.Row="1"
                                    Grid.Column="0"
                                    Margin="0,0,10,5"
                                    Text="{DynamicResource Turing_UsedSpace}" />
                                <TextBlock
                                    Grid.Row="1"
                                    Grid.Column="1"
                                    Margin="0,0,0,5"
                                    Text="{Binding UsedStorageDisplay}" />

                                <TextBlock
                                    Grid.Row="2"
                                    Grid.Column="0"
                                    Margin="0,0,10,0"
                                    Text="{DynamicResource Turing_FreeSpace}" />
                                <TextBlock
                                    Grid.Row="2"
                                    Grid.Column="1"
                                    Text="{Binding FreeStorageDisplay}" />
                            </Grid>

                            <ProgressBar
                                Height="10"
                                Margin="0,10,0,0"
                                Maximum="100"
                                Value="{Binding StorageUsagePercentage}" />
                        </StackPanel>
                    </ui:Card>

                    <!--  Action Buttons  -->
                    <StackPanel
                        Grid.Row="1"
                        Margin="0,0,0,10"
                        Orientation="Horizontal">
                        <ui:Button
                            Margin="0,0,10,0"
                            Appearance="Secondary"
                            Command="{Binding RefreshStorageCommand}"
                            Content="{DynamicResource Common_Refresh}"
                            Icon="{ui:SymbolIcon ArrowSync24}" />
                        <ui:Button
                            Margin="0,0,10,0"
                            Appearance="Primary"
                            Command="{Binding UploadVideoCommand}"
                            Content="{DynamicResource Turing_UploadVideo}"
                            Icon="{ui:SymbolIcon VideoClip24}" />
                        <TextBlock
                            Margin="10,0,0,0"
                            VerticalAlignment="Center"
                            FontStyle="Italic"
                            Text="{Binding CurrentlyPlaying, StringFormat='Currently Playing: {0}', FallbackValue='Currently Playing: Nothing'}" />
                    </StackPanel>

                    <!--  File List  -->
                    <ui:Card Grid.Row="2">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <TextBlock
                                Grid.Row="0"
                                Margin="0,0,0,10"
                                FontSize="13"
                                FontWeight="Medium"
                                Text="{DynamicResource Turing_VideoFiles}" />

                            <DataGrid
                                Grid.Row="1"
                                AutoGenerateColumns="False"
                                CanUserAddRows="False"
                                GridLinesVisibility="Horizontal"
                                HeadersVisibility="Column"
                                IsReadOnly="True"
                                ItemsSource="{Binding DeviceFiles}"
                                SelectedItem="{Binding SelectedFile}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn
                                        Width="*"
                                        Binding="{Binding Name}"
                                        Header="{DynamicResource Common_Name}" />
                                    <DataGridTextColumn
                                        Width="80"
                                        Binding="{Binding Type}"
                                        Header="{DynamicResource Common_Type}" />
                                    <DataGridTextColumn
                                        Width="100"
                                        Binding="{Binding SizeDisplay}"
                                        Header="{DynamicResource Common_Size}" />
                                    <DataGridTemplateColumn Width="200" Header="{DynamicResource Common_Actions}">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <ui:Button
                                                        Margin="0,0,5,0"
                                                        Appearance="Secondary"
                                                        Command="{Binding DataContext.PlayFileCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                        CommandParameter="{Binding}"
                                                        Content="{DynamicResource Common_Play}"
                                                        FontSize="12" />
                                                    <ui:Button
                                                        Appearance="Danger"
                                                        Command="{Binding DataContext.DeleteFileCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                        CommandParameter="{Binding}"
                                                        Content="{DynamicResource Common_Delete}"
                                                        FontSize="12" />
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </ui:Card>

                    <!--  Status Bar Container with fixed height  -->
                    <Grid
                        Grid.Row="3"
                        Height="60"
                        Margin="0,10,0,0">
                        <ui:InfoBar
                            Title="{Binding StatusTitle}"
                            VerticalAlignment="Top"
                            IsOpen="{Binding IsStatusVisible}"
                            Message="{Binding StatusMessage}"
                            Severity="{Binding StatusSeverity}" />
                    </Grid>
                </Grid>
            </TabItem>

            <!--  Settings Tab  -->
            <TabItem Header="{DynamicResource Nav_Settings}">
                <Grid Margin="10">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <!--  Device Settings  -->
                            <ui:Card Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock
                                        Margin="0,0,0,10"
                                        FontSize="13"
                                        FontWeight="Medium"
                                        Text="{DynamicResource Turing_DeviceSettings}" />

                                    <TextBlock Margin="0,0,0,5" Text="{DynamicResource Turing_DefaultBrightness}" />
                                    <Grid Margin="0,0,0,15">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="50" />
                                        </Grid.ColumnDefinitions>
                                        <Slider
                                            Grid.Column="0"
                                            Margin="0,0,10,0"
                                            IsSnapToTickEnabled="True"
                                            Maximum="100"
                                            Minimum="0"
                                            TickFrequency="5"
                                            Value="{Binding Brightness}" />
                                        <TextBlock
                                            Grid.Column="1"
                                            VerticalAlignment="Center"
                                            Text="{Binding Brightness, StringFormat='{}{0}%'}" />
                                    </Grid>

                                    <TextBlock Margin="0,0,0,5" Text="{DynamicResource Turing_DefaultOrientation}" />
                                    <ComboBox
                                        MinWidth="200"
                                        Margin="0,0,0,15"
                                        HorizontalAlignment="Left"
                                        ItemsSource="{Binding OrientationOptions}"
                                        SelectedItem="{Binding SelectedOrientation}" />

                                    <TextBlock Margin="0,0,0,5" Text="{DynamicResource Turing_StartupMode}" />
                                    <ComboBox
                                        MinWidth="200"
                                        Margin="0,0,0,10"
                                        HorizontalAlignment="Left"
                                        ItemsSource="{Binding StartupModeOptions}"
                                        SelectedItem="{Binding SelectedStartupMode}" />

                                    <TextBlock
                                        Margin="0,0,0,10"
                                        FontSize="12"
                                        FontStyle="Italic"
                                        Text="{DynamicResource Turing_StartupModeNote}"
                                        TextWrapping="Wrap" />
                                </StackPanel>
                            </ui:Card>

                            <!--  Device Actions  -->
                            <ui:Card>
                                <StackPanel>
                                    <TextBlock
                                        Margin="0,0,0,10"
                                        FontSize="13"
                                        FontWeight="Medium"
                                        Text="{DynamicResource Turing_DeviceActions}" />

                                    <WrapPanel>
                                        <ui:Button
                                            Margin="0,0,10,10"
                                            Appearance="Primary"
                                            Command="{Binding SaveSettingsCommand}"
                                            Content="{DynamicResource Turing_SaveSettings}"
                                            Icon="{ui:SymbolIcon Save24}" />
                                        <ui:Button
                                            Margin="0,0,10,10"
                                            Appearance="Secondary"
                                            Command="{Binding RestartDeviceCommand}"
                                            Content="{DynamicResource Turing_RestartDevice}"
                                            Icon="{ui:SymbolIcon ArrowReset24}" />
                                    </WrapPanel>
                                </StackPanel>
                            </ui:Card>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </TabItem>
        </TabControl>

        <!--  Loading Overlay  -->
        <Grid
            Grid.Row="1"
            Background="#80000000"
            Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ui:ProgressRing
                    Width="50"
                    Height="50"
                    IsIndeterminate="True" />
                <TextBlock
                    Margin="0,10,0,0"
                    HorizontalAlignment="Center"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Text="{Binding LoadingText}" />
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>